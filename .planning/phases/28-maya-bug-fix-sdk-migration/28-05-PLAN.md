---
phase: 28-maya-bug-fix-sdk-migration
plan: 05
type: execute
wave: 2
depends_on:
  - 28-02
  - 28-04
files_modified:
  - server/routes/chat.ts
autonomous: true
requirements:
  - BUG-05
must_haves:
  truths:
    - "After `handleStreamingColleagueResponse` exits (any path: success, error, abort), `(ws as any).__currentAbortController` is `undefined` — verified by `scripts/test-abort-cleanup.ts`"
    - "After 50 sequential aborted streams against a HangingProvider, the V8 heap delta is < 50MB — verified by `scripts/test-abort-heap.ts`"
    - "The finally block in `handleStreamingColleagueResponse` runs regardless of which catch fired (including the inner abort-aware guard from plan 28-04)"
  artifacts:
    - path: "server/routes/chat.ts"
      provides: "AbortController reference cleanup in handleStreamingColleagueResponse finally block"
      contains: "delete (ws as any).__currentAbortController"
  key_links:
    - from: "server/routes/chat.ts (handleStreamingColleagueResponse finally — line ~3021)"
      to: "ws object property cleanup"
      via: "delete operator"
      pattern: "delete\\s*\\(ws as any\\)\\.__currentAbortController"
---

<objective>
Close BUG-05: AbortController reference accumulates on the WS object across requests. Add a single `delete (ws as any).__currentAbortController;` line to the existing `finally` block of `handleStreamingColleagueResponse` so the reference is cleared after every stream completes.

Purpose: Plan 28-02 already ensures the SDK call honors AbortSignal (so the underlying HTTP request closes promptly). The remaining leak is metadata: the AbortController object itself is attached to the WS as a property, never deleted. Across many requests, these accumulate — and although the WS object is GC'd when the connection closes, long-lived sessions hold many AbortController references.

Output: One file modified (`server/routes/chat.ts`) with a single line added to the finally block. After this plan: `ws.__currentAbortController` is properly cleaned up, Wave 0 cleanup test green, heap test confirms < 50MB delta under load.

This plan is INDEPENDENT of plans 28-03 and 28-04 (separate file regions). Depends only on 28-02 because the heap test relies on the SDK actually honoring AbortSignal (otherwise the test would always pass / fail for the wrong reason).
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-02-SUMMARY.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-VALIDATION.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/CLAUDE.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/server/routes/chat.ts

<interfaces>
<!-- The exact existing code being modified. Line numbers verified 2026-04-26. -->

CURRENT lines 1901-1903 (AbortController creation — for context, NOT modified):
```typescript
// Create AbortController for cancellation + WS disconnect
const abortController = new AbortController();
(ws as any).__currentAbortController = abortController;
```

CURRENT lines 3021-3023 (finally block at the END of handleStreamingColleagueResponse):
```typescript
} finally {
  ws.off('message', cancelHandler);
}
```

This is the ONLY finally block at this scope (the body of `handleStreamingColleagueResponse`). The function spans roughly lines 1850-3024. There is also a separate finally at lines 982-986 inside the `case 'send_message_streaming':` block — that one is OUT OF SCOPE for this plan (it's the outer one, not inside handleStreamingColleagueResponse).

To distinguish them: the OUTER finally at lines 982-986 has `activeStreamingResponses.delete(envelope.conversationId)`. The INNER finally at lines 3021-3023 has `ws.off('message', cancelHandler)`. We're modifying the INNER one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete `(ws as any).__currentAbortController` in handleStreamingColleagueResponse finally block</name>
  <read_first>
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md (lines 325-372 — AbortController Cleanup section; lines 366-372 — exact fix)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/routes/chat.ts (lines 3015-3024 — the finally block being modified; lines 1901-1917 — surrounding context for `__currentAbortController` setup)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/scripts/test-abort-cleanup.ts (Wave 0 test from plan 28-01 — turns green when this fix lands)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/scripts/test-abort-heap.ts (Wave 0 load test from plan 28-01 — turns green after this fix + 28-02 land)
  </read_first>
  <files>server/routes/chat.ts</files>
  <action>
    Locate the INNER finally block at lines 3021-3023:
    ```typescript
    } finally {
      ws.off('message', cancelHandler);
    }
    ```

    To uniquely locate it: search for the exact 3-line sequence `} finally {\n  ws.off('message', cancelHandler);\n}` — there is only ONE match in chat.ts.

    Add a single line AFTER the existing `ws.off(...)` call. The result MUST be:
    ```typescript
    } finally {
      ws.off('message', cancelHandler);
      // BUG-05: clear the AbortController reference so it does not accumulate on the WS object.
      delete (ws as any).__currentAbortController;
    }
    ```

    Constraints:
    1. Do NOT modify the existing `ws.off('message', cancelHandler);` line.
    2. Do NOT touch the OUTER finally block at lines 982-986 (it has different cleanup concerns: `activeStreamingResponses.delete`, `streamingConversations.delete`).
    3. The `delete` operator must use the `(ws as any)` cast — same pattern as line 1903 where the property is set.

    After editing:
    1. Run `npm run typecheck` — must pass (delete on `any`-cast property is type-safe)
    2. Run `tsx scripts/test-abort-cleanup.ts` — was red in plan 28-01, must now be green
    3. Run `node --expose-gc $(which tsx) scripts/test-abort-heap.ts` — was red in plan 28-01, must now be green (depends on plan 28-02 having landed AbortSignal handling in geminiProvider)

    Verify nothing else in the codebase references `__currentAbortController` (it should only be set at line 1903 and now deleted at the new line). Run:
    ```bash
    grep -n "__currentAbortController" server/routes/chat.ts
    ```
    Expected: 4 matches (1 declaration at line 1903, 2 reads in the `case 'send_message_streaming':` outer catch + cancel handler at lines 971, 994, and 1 new delete in the finally).
  </action>
  <verify>
    <automated>grep -c "delete (ws as any).__currentAbortController" server/routes/chat.ts | awk '{ if ($1 -lt 1) exit 1; print "matches:", $1 }' && npm run typecheck && tsx scripts/test-abort-cleanup.ts && node --expose-gc $(which tsx) scripts/test-abort-heap.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/routes/chat.ts` contains the literal string `delete (ws as any).__currentAbortController` exactly ONCE
    - The delete statement appears INSIDE a finally block — confirmed by `grep -B 2 "delete (ws as any).__currentAbortController" server/routes/chat.ts` showing `} finally {` within the 2 preceding lines
    - The delete statement appears IMMEDIATELY AFTER (within 2 lines of) `ws.off('message', cancelHandler)`
    - The OUTER finally block (containing `activeStreamingResponses.delete(envelope.conversationId)`) is UNCHANGED — verify with `grep -A 3 "activeStreamingResponses.delete" server/routes/chat.ts` showing only `streamingConversations.delete` follows, NOT the new __currentAbortController delete
    - `npm run typecheck` exits 0
    - `tsx scripts/test-abort-cleanup.ts` exits 0 (was red in plan 28-01, now green)
    - `node --expose-gc $(which tsx) scripts/test-abort-heap.ts` exits 0 (was red in plan 28-01, now green — heap delta < 50MB after 50 aborts)
    - Total lines changed in chat.ts ≤ 3 (one comment + one delete + maybe trailing whitespace)
  </acceptance_criteria>
  <done>One-line cleanup added to inner finally. Wave 0 cleanup test green. Wave 0 heap test green (under 50MB delta after 50 concurrent aborts).</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `tsx scripts/test-abort-cleanup.ts` exits 0
- `node --expose-gc $(which tsx) scripts/test-abort-heap.ts` exits 0 with logged heap delta < 50MB
- Manual smoke: `npm run dev`, send a message, click stop, send another message, click stop. Inspect `ws.__currentAbortController` via Node debugger or `node --inspect` — should be `undefined` between requests.
- No regression: existing tests `npx playwright test tests/e2e/chat-redesign.spec.ts` still pass
</verification>

<success_criteria>
- BUG-05 closed: AbortController reference is deleted from the WS object after every stream completes (any exit path: success, error, abort)
- Heap leak verified absent: 50 concurrent aborts produce < 50MB heap delta
- Wave 0 tests `test-abort-cleanup.ts` and `test-abort-heap.ts` are now green
- Surgical change: ≤ 3 lines added to chat.ts, no existing logic modified
</success_criteria>

<output>
After completion, create `.planning/phases/28-maya-bug-fix-sdk-migration/28-05-SUMMARY.md` documenting:
- The exact line where the delete was added
- The heap delta observed by `test-abort-heap.ts` (e.g. "27MB delta after 50 aborts — well below 50MB threshold")
- Whether `--expose-gc` flag was required for the heap test to be reliable (note for future ops)
- Any unrelated changes accidentally introduced (should be ZERO)
</output>
