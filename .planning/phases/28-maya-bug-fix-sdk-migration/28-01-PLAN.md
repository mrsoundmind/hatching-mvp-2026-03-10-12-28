---
phase: 28-maya-bug-fix-sdk-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-abort-signal-propagation.ts
  - scripts/test-gemini-abort.ts
  - scripts/test-streaming-timeout-cleanup.ts
  - scripts/test-abort-cleanup.ts
  - scripts/test-abort-heap.ts
  - tests/e2e/stop-button.spec.ts
  - tests/e2e/maya-fallback.spec.ts
autonomous: true
requirements: []  # Wave 0 scaffolding for BUG-01..06 (no requirements directly closed; enables verification)
must_haves:
  truths:
    - "Every BUG-XX requirement has at least one automated test script or Playwright spec that fails today (red state) and will turn green after Wave 1/2 fixes land"
    - "Test scripts use the project's existing tsx + scripts/ pattern (no new test framework introduced)"
    - "Playwright specs follow the existing tests/e2e/ pattern (auth.setup.ts, helpers.ts) and run via `npx playwright test`"
  artifacts:
    - path: "scripts/test-abort-signal-propagation.ts"
      provides: "Unit test asserting LLMRequest.signal flows from openaiService.ts through streamChatWithRuntimeFallback to a mock provider"
      min_lines: 60
    - path: "scripts/test-gemini-abort.ts"
      provides: "Integration test with a mock provider that hangs; asserts the streamChat generator terminates within 31s when AbortSignal.timeout(30_000) fires"
      min_lines: 60
    - path: "scripts/test-streaming-timeout-cleanup.ts"
      provides: "WS integration test asserting typing_stopped fires before streaming_error on the timeout path"
      min_lines: 80
    - path: "scripts/test-abort-cleanup.ts"
      provides: "Unit test asserting (ws as any).__currentAbortController is undefined after handleStreamingColleagueResponse finally block runs"
      min_lines: 50
    - path: "scripts/test-abort-heap.ts"
      provides: "Load test using v8.getHeapStatistics() asserting heap delta < 50MB after 50 concurrent aborted streams"
      min_lines: 80
    - path: "tests/e2e/stop-button.spec.ts"
      provides: "Playwright E2E with 3 scenarios (success completion / user cancel / streaming_error) — asserts stop button reverts to send within 1s of terminal stream event"
      min_lines: 90
    - path: "tests/e2e/maya-fallback.spec.ts"
      provides: "Playwright E2E asserting no 'out for lunch'/'resting circuits' text appears for a 4-8s slow response, but DOES appear (as inline error) on a true 30s+ timeout"
      min_lines: 80
  key_links:
    - from: "scripts/test-abort-signal-propagation.ts"
      to: "server/llm/providerResolver.ts (streamChatWithRuntimeFallback)"
      via: "spy provider registered into providerRegistry"
      pattern: "spyProvider\\.streamChat.*calledWith.*signal"
    - from: "tests/e2e/stop-button.spec.ts"
      to: "client/src/components/CenterPanel.tsx (chat input button)"
      via: "playwright getByRole('button')"
      pattern: "(aria-label=['\"]Send['\"]|data-testid=['\"]send-button['\"])"
---

<objective>
Create the Wave 0 test infrastructure that verifies all 6 BUG-XX requirements. Every script/spec must run today and FAIL (red) — that's the baseline. After plans 28-02..05 land, every test must pass (green).

Purpose: Without these tests, plans 28-02..05 cannot self-verify. The research (28-RESEARCH.md Validation Architecture section) explicitly enumerates all 7 missing test files. This plan creates them in a single concentrated effort.

Output: 5 tsx scripts under `scripts/` + 2 Playwright specs under `tests/e2e/`. All tests are red today and will be the gating verification for Wave 1/2 fixes.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-VALIDATION.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/CLAUDE.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/playwright.config.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/tests/e2e/helpers.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/tests/e2e/auth.setup.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/scripts/test-budget-race.ts

<interfaces>
<!-- Key contracts the test scripts will exercise. Use these directly — do NOT explore the codebase to rediscover them. -->

From server/llm/providerTypes.ts (current state — `signal` field is INTENTIONALLY ABSENT and will be added in plan 28-02):
```typescript
export interface LLMRequest {
  messages: ChatMessage[];
  model?: string;
  modelTier?: ModelTier;
  temperature?: number;
  maxTokens?: number;
  timeoutMs?: number;
  seed?: number;
  // signal?: AbortSignal;  // ← will be added by plan 28-02
}

export interface LLMProvider {
  readonly id: ProviderId;
  generateChat(request: LLMRequest, mode: RuntimeMode): Promise<LLMGenerationResult>;
  streamChat(request: LLMRequest, mode: RuntimeMode): Promise<LLMStreamResult>;
  healthCheck?(model?: string): Promise<ProviderHealth>;
}

export interface LLMStreamResult {
  stream: AsyncGenerator<string, void, unknown>;
  metadata: LLMResponseMetadata;
}
```

From server/llm/providerResolver.ts (registry pattern to inject a spy):
```typescript
const providerRegistry: Record<ProviderId, LLMProvider> = {
  openai: openaiProvider,
  gemini: geminiProvider,
  groq: groqProvider,
  'ollama-test': ollamaProvider,
  mock: mockProvider,
};
```

From tests/e2e/auth.setup.ts (auth fixture pattern — Playwright specs must reuse this):
```typescript
// Specs use: test.use({ storageState: 'tests/e2e/.auth/user.json' })
// or `setup as authSetup` from auth.setup.ts
```

From scripts/test-budget-race.ts (canonical pattern for tsx scripts):
```typescript
// Header: `#!/usr/bin/env tsx`
// Body: async main() with assert calls; exit 0 on success, throw on failure
// Run via: tsx scripts/test-budget-race.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/test-abort-signal-propagation.ts — assert LLMRequest.signal flows end-to-end</name>
  <read_first>
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md (lines 130-188 — AbortSignal Propagation section)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providerTypes.ts (full file — current LLMRequest shape)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providerResolver.ts (lines 1-120 — registry + streamChatWithRuntimeFallback signature)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providers/mockProvider.ts (existing mock provider pattern to mimic)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/scripts/test-budget-race.ts (canonical script structure)
  </read_first>
  <files>scripts/test-abort-signal-propagation.ts</files>
  <action>
    Create a new tsx script that proves `LLMRequest.signal` flows from a caller all the way down to the provider.streamChat() call. The script:

    1. Builds a `SpyProvider implements LLMProvider` whose `streamChat(request, mode)` method records `request.signal` to a module-level `capturedSignal` variable, then yields one synthetic chunk and returns. Do NOT register this provider in the real registry — instead call its `streamChat` method directly to keep the test isolated.

    2. Imports `streamChatWithRuntimeFallback` from `server/llm/providerResolver.ts` and observes that the function passes the request unchanged to the resolved provider (without dropping the `signal` field). To verify this, monkey-patch `providerRegistry.mock` (using `(providerRegistry as any).mock = spyProvider`) BEFORE invoking, with `LLM_MODE=test` and `TEST_LLM_PROVIDER=mock`.

    3. Asserts (using Node `assert/strict`):
       - `capturedSignal !== undefined`
       - `capturedSignal instanceof AbortSignal`
       - The signal aborts when the caller's controller fires (set up controller, abort it after 100ms, assert `capturedSignal.aborted === true` after 200ms wait)

    4. The script MUST exit 0 on pass and throw on fail. NO Vitest. Plain `assert` + `console.log('PASS')`.

    Header line: `#!/usr/bin/env tsx`. Use `import 'dotenv/config'` if env vars are needed. Set `process.env.LLM_MODE = 'test'`, `process.env.TEST_LLM_PROVIDER = 'mock'` before importing `providerResolver`.

    Important: today (before plan 28-02 lands) `LLMRequest` has NO `signal` field. The script will fail with a TypeScript error or `capturedSignal === undefined`. That is the expected red state. The script will turn green only after plan 28-02-01 adds `signal?: AbortSignal` and 28-02-03 wires it through openaiService.ts.

    Add a comment at the top of the file:
    ```
    /**
     * Wave 0 test for BUG-02. RED until plan 28-02 lands.
     * Verifies LLMRequest.signal flows from caller → providerResolver → provider.streamChat.
     */
    ```
  </action>
  <verify>
    <automated>tsx scripts/test-abort-signal-propagation.ts; rc=$?; if [ $rc -ne 0 ]; then echo "EXPECTED RED — turns green after plan 28-02"; fi; test -f scripts/test-abort-signal-propagation.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/test-abort-signal-propagation.ts` exists and is at least 60 lines
    - File starts with `#!/usr/bin/env tsx`
    - File contains the string `class SpyProvider`
    - File contains the string `capturedSignal`
    - File contains the string `assert.strictEqual` or `assert.ok` (uses `node:assert/strict`)
    - File contains the string `streamChatWithRuntimeFallback`
    - File contains the string `AbortController`
    - File imports from `'../server/llm/providerResolver.js'` (note `.js` extension for ESM compatibility)
    - Running `tsx scripts/test-abort-signal-propagation.ts` exits non-zero TODAY (red baseline) — captured by failing on `capturedSignal === undefined` assertion
  </acceptance_criteria>
  <done>Script created, runs as tsx, contains spy + assertions on `request.signal`, fails today as expected.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/test-gemini-abort.ts + scripts/test-streaming-timeout-cleanup.ts + scripts/test-abort-cleanup.ts + scripts/test-abort-heap.ts — backend BUG-02/03/05 scaffolds</name>
  <read_first>
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md (lines 320-410 — AbortController Cleanup + WebSocket Event Ordering Contract)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providers/geminiProvider.ts (full file — current implementation)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/routes/chat.ts (lines 1900-1920 — abortController setup; lines 950-986 — outer race + catch; lines 3015-3024 — finally block)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/scripts/test-budget-race.ts (concurrency test pattern)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providers/mockProvider.ts (mock provider pattern)
  </read_first>
  <files>scripts/test-gemini-abort.ts, scripts/test-streaming-timeout-cleanup.ts, scripts/test-abort-cleanup.ts, scripts/test-abort-heap.ts</files>
  <action>
    Create FOUR tsx scripts (each starts with `#!/usr/bin/env tsx`, uses `node:assert/strict`):

    --- (A) scripts/test-gemini-abort.ts ---

    Purpose: prove that when `AbortSignal.timeout(30_000)` fires inside the Gemini stream provider, the generator terminates within 31s.

    Strategy: build a `HangingProvider implements LLMProvider` whose `streamChat` returns a generator that hangs by yielding a never-resolving Promise UNLESS `signal.aborted` becomes true. Do NOT touch real Gemini SDK. Inject the hanging provider directly (not through registry) — call `hangingProvider.streamChat({ ..., signal: AbortSignal.timeout(2_000) }, 'test')` and iterate, measuring `Date.now()` from start to generator end. Assert end-to-start < 3000ms (use 2s in test for fast CI; the production path uses 30s).

    The script must contain a comment:
    ```
    /**
     * Wave 0 test for BUG-02. RED until plan 28-02 adds AbortSignal handling to geminiProvider.
     * NOTE: This test uses HangingProvider (not real Gemini) to avoid network/keys; the contract
     * being tested is "any provider given AbortSignal MUST honor it within 1s of fire".
     */
    ```

    Test variants (sub-tests inside the file):
    1. With AbortSignal.timeout(2_000), generator ends within 2.5s — PASS once provider honors signal
    2. With manual AbortController.abort() after 500ms, generator ends within 1s — PASS once provider honors signal
    3. With NO signal, generator hangs (timeout via Promise.race after 5s) — sanity check that the test infra works

    --- (B) scripts/test-streaming-timeout-cleanup.ts ---

    Purpose: WS-level integration test asserting `typing_stopped` fires BEFORE `streaming_error` on the timeout/error path.

    Strategy: Spin up a minimal in-process WebSocket server using `import { WebSocketServer } from 'ws'`. Connect a client. Simulate the chat.ts catch-block sequence (reproduce the relevant ~30 lines from chat.ts:968-981 inline in the test, OR use a stub helper that mirrors the production code). Send a `send_message_streaming` envelope, wait for events, capture the order.

    Assertions:
    - Client receives `typing_stopped` event
    - Client receives `streaming_error` event
    - The index of `typing_stopped` in the captured array is LESS THAN the index of `streaming_error`

    The script must include a comment:
    ```
    /**
     * Wave 0 test for BUG-03. RED until plan 28-04 emits typing_stopped before streaming_error.
     */
    ```

    --- (C) scripts/test-abort-cleanup.ts ---

    Purpose: prove `(ws as any).__currentAbortController` is `undefined` after `handleStreamingColleagueResponse` finally block executes.

    Strategy: Build a fake WS object (`const fakeWs: any = new EventEmitter(); fakeWs.send = () => {}; fakeWs.off = () => {}; fakeWs.on = () => {};`). Set `fakeWs.__currentAbortController = new AbortController()`. Simulate the finally block executing (call a stub function that mirrors the production cleanup). Assert `fakeWs.__currentAbortController === undefined`.

    Today: the production code at chat.ts:3022 only does `ws.off('message', cancelHandler)` — does NOT delete `__currentAbortController`. Plan 28-05 will add the delete. So this test fails today (red), passes after 28-05.

    --- (D) scripts/test-abort-heap.ts ---

    Purpose: load test asserting heap delta < 50MB after 50 concurrent aborted streams.

    Strategy: Use the same `HangingProvider` from (A). Loop 50 times in parallel, each: create AbortController, call `hangingProvider.streamChat({ ..., signal: controller.signal }, 'test')`, abort after 100ms, await iteration end. Use `v8.getHeapStatistics()` before and after, plus `global.gc?.()` between (note: requires `--expose-gc` flag; if not available, skip GC step but still measure).

    Assertion: `heapAfter.used_heap_size - heapBefore.used_heap_size < 50 * 1024 * 1024`.

    The script must include a comment:
    ```
    /**
     * Wave 0 test for BUG-05. RED until plan 28-02 + 28-05 land (signal handling + finally cleanup).
     * Run with: node --expose-gc $(which tsx) scripts/test-abort-heap.ts
     */
    ```

    All four scripts: header `#!/usr/bin/env tsx`, exit 0 on pass, throw or `process.exit(1)` on fail, log `console.log('PASS')` at end.
  </action>
  <verify>
    <automated>test -f scripts/test-gemini-abort.ts && test -f scripts/test-streaming-timeout-cleanup.ts && test -f scripts/test-abort-cleanup.ts && test -f scripts/test-abort-heap.ts && wc -l scripts/test-gemini-abort.ts scripts/test-streaming-timeout-cleanup.ts scripts/test-abort-cleanup.ts scripts/test-abort-heap.ts && tsx scripts/test-abort-cleanup.ts; rc=$?; if [ $rc -eq 0 ]; then echo "ERROR: test-abort-cleanup.ts must exit non-zero (red baseline) before plan 28-05 turns it green"; exit 1; fi; tsx scripts/test-abort-heap.ts; rc=$?; if [ $rc -eq 0 ]; then echo "ERROR: test-abort-heap.ts must exit non-zero (red baseline) before plan 28-05 turns it green"; exit 1; fi; echo "RED baseline confirmed for cleanup + heap scripts"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `scripts/`
    - `scripts/test-gemini-abort.ts` is at least 60 lines and contains the strings `HangingProvider`, `AbortSignal.timeout`, `Date.now()` (for timing assertion)
    - `scripts/test-streaming-timeout-cleanup.ts` is at least 80 lines and contains the strings `WebSocketServer`, `typing_stopped`, `streaming_error`, and asserts ordering with `<` operator
    - `scripts/test-abort-cleanup.ts` is at least 50 lines and contains the strings `__currentAbortController`, `assert.strictEqual.*undefined`
    - `scripts/test-abort-heap.ts` is at least 80 lines and contains the strings `getHeapStatistics`, `50 * 1024 * 1024`, `Promise.all`
    - Each file starts with `#!/usr/bin/env tsx` on line 1
    - Each file imports from `node:assert/strict`
    - Running `tsx scripts/test-abort-cleanup.ts` exits non-zero today (red baseline expected)
  </acceptance_criteria>
  <done>Four scripts created. Each is red today (baseline confirmed). Each will turn green after the matching plan in Wave 1/2 lands.</done>
</task>

<task type="auto">
  <name>Task 3: tests/e2e/stop-button.spec.ts + tests/e2e/maya-fallback.spec.ts — Playwright specs for BUG-04 and BUG-06</name>
  <read_first>
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/28-maya-bug-fix-sdk-migration/28-RESEARCH.md (lines 244-323 — BUG-06 Stop Button Stuck Analysis)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/playwright.config.ts (test config, baseURL, projects)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/tests/e2e/auth.setup.ts (auth fixture)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/tests/e2e/helpers.ts (existing helpers)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/tests/e2e/chat-redesign.spec.ts (chat input pattern reference)
    - /Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/components/CenterPanel.tsx (lines 285-330 — truth-enforcer; line 1937 — derived isStreaming; chat input button area)
  </read_first>
  <files>tests/e2e/stop-button.spec.ts, tests/e2e/maya-fallback.spec.ts</files>
  <action>
    Create TWO Playwright specs.

    --- (A) tests/e2e/stop-button.spec.ts ---

    Use `@playwright/test`. Reuse the existing auth storageState pattern from `tests/e2e/auth.setup.ts`. Three test cases inside one `test.describe('BUG-06 stop button reverts to send within 1s')`:

    1. `test('after streaming_completed (success path)', async ({ page }) => { ... })`
       - Navigate to a project chat (use helpers.ts navigation helper if present; otherwise hard-code a known project URL `/maya/test-project` after auth)
       - Type a short user message, press Enter
       - Wait for at least one assistant message to appear in DOM (locator: `[data-testid="message-bubble"][data-message-type="agent"]` OR `.message-bubble` with agent class — adapt to actual DOM)
       - Capture timestamp T0 = moment last assistant message lands
       - Within 1000ms of T0, assert the chat input button has `aria-label="Send"` (not "Stop"). Use `page.getByRole('button', { name: 'Send' })` with timeout 1000

    2. `test('after user clicks stop button (cancel path)', async ({ page }) => { ... })`
       - Type a message that triggers a long response (use a complex prompt e.g. "Write me a 2000-word story")
       - Wait until button shows "Stop" (`page.getByRole('button', { name: 'Stop' })`)
       - Click stop
       - Within 1000ms, assert button reverts to "Send" via `page.getByRole('button', { name: 'Send' }).waitFor({ timeout: 1000 })`

    3. `test('after streaming_error (error path)', async ({ page }) => { ... })`
       - Use Playwright route mocking to force the WS to drop or return an error: `await page.route('**/ws', route => route.abort())` is one option; alternatively send a message that triggers `STREAMING_HARD_TIMEOUT` by stubbing fetch
       - The simplest implementation: skip route mocking and instead trigger a real error by setting a very short hard timeout via `process.env.HARD_RESPONSE_TIMEOUT_MS=1000` in a beforeAll hook (but env can't be set per-test in Playwright runtime). Pragmatic alternative: mark this test `test.skip` with a TODO comment if reliable error injection is too brittle, but include the test SKELETON so plan 28-04 can complete it.
       - Within 1000ms of error appearing in DOM, assert button shows "Send"

    Test must use `expect.toBeVisible({ timeout: 1000 })` for the 1s assertion. The `1000ms` budget is the BUG-06 contract.

    Header comment:
    ```
    /**
     * Wave 0 spec for BUG-06. RED until plan 28-03 reduces the truth-enforcer debounce + forces
     * metadata.isStreaming=false in the new_message handler.
     */
    ```

    --- (B) tests/e2e/maya-fallback.spec.ts ---

    Two test cases inside `test.describe('BUG-04 fallback never appears for valid latency')`:

    1. `test('no out-for-lunch text on a 4-8s slow response', async ({ page }) => { ... })`
       - Send a message likely to take 4-8 seconds (e.g. a multi-part question requiring Gemini Pro)
       - Wait for the response to complete
       - Assert NEITHER `/out for lunch/i` NOR `/resting (their|her|his) circuits/i` appears anywhere in the chat panel: `await expect(page.locator('body')).not.toContainText(/out for lunch|resting (their|her|his) circuits/i)`
       - Use `test.setTimeout(45_000)` to allow real LLM response time

    2. `test('proper inline error appears on >30s timeout (skip if no error injection)', async ({ page }) => { ... })`
       - Mark this test `test.skip` if reliable error injection requires server-side env changes — include a TODO comment explaining the test will be enabled after plan 28-02 lands and the `HARD_RESPONSE_TIMEOUT_MS` knob can be set via querystring / dev-only header

    Header comment:
    ```
    /**
     * Wave 0 spec for BUG-04. RED until plan 28-04 adds abortController.signal.aborted guard before
     * the buildServiceFallbackMessage call in chat.ts inner catch.
     */
    ```

    Both specs must use the existing storageState fixture (look at `tests/e2e/chat-redesign.spec.ts` for the canonical `test.use({ storageState: 'tests/e2e/.auth/user.json' })` pattern — match it exactly).
  </action>
  <verify>
    <automated>test -f tests/e2e/stop-button.spec.ts && test -f tests/e2e/maya-fallback.spec.ts && npx playwright test --list tests/e2e/stop-button.spec.ts tests/e2e/maya-fallback.spec.ts 2>&1 | grep -E "test|describe"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/stop-button.spec.ts` exists, is at least 90 lines, contains `BUG-06` and 3 `test(` blocks (success, cancel, error)
    - File `tests/e2e/maya-fallback.spec.ts` exists, is at least 80 lines, contains `BUG-04` and uses `not.toContainText` with regex matching `out for lunch|resting`
    - Both files import from `@playwright/test`
    - Both files reference `storageState` (auth fixture reuse)
    - `npx playwright test --list tests/e2e/stop-button.spec.ts tests/e2e/maya-fallback.spec.ts` succeeds (parser-level: tests are discoverable)
    - Stop-button spec contains the string `aria-label="Send"` or `getByRole.*Send`
    - Stop-button spec contains the string `1000` (the 1-second budget)
  </acceptance_criteria>
  <done>Two Playwright specs created. Discoverable by `playwright test --list`. Each is red today and gates Wave 1/2 plan completion.</done>
</task>

</tasks>

<verification>
- All 7 Wave 0 files exist
- All scripts run (or fail in expected red state) without syntax errors: `for f in scripts/test-abort-signal-propagation.ts scripts/test-gemini-abort.ts scripts/test-streaming-timeout-cleanup.ts scripts/test-abort-cleanup.ts scripts/test-abort-heap.ts; do tsx --no-warnings $f 2>&1 | head -5; done`
- Playwright specs are discoverable: `npx playwright test --list tests/e2e/stop-button.spec.ts tests/e2e/maya-fallback.spec.ts`
- TypeScript compiles: `npm run typecheck` passes (Wave 0 introduces no type errors in production code; the failing assertions are runtime-level)
</verification>

<success_criteria>
- 7 test files created (5 tsx scripts + 2 Playwright specs)
- Every file has a header comment naming the BUG-XX requirement it gates
- Every file is RED today (verified by running and observing non-zero exit / failed assertion)
- Every file is structured to turn GREEN automatically once the corresponding fix lands
- TypeScript typecheck passes (no production type errors introduced)
- No new test framework or dev dep added (Playwright + tsx already installed)
</success_criteria>

<output>
After completion, create `.planning/phases/28-maya-bug-fix-sdk-migration/28-01-SUMMARY.md` documenting:
- Each Wave 0 file path and the BUG-XX requirement it gates
- The exact failing assertion in each (e.g. "test-abort-signal-propagation.ts fails with `capturedSignal === undefined` because LLMRequest.signal does not yet exist")
- The plan ID that will turn each test green (e.g. "test-abort-signal-propagation.ts → green after 28-02")
</output>
