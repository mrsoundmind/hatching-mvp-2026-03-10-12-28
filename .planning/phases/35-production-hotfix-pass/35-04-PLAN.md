---
phase: 35-production-hotfix-pass
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - client/src/pages/legal/PrivacyPage.tsx
  - client/src/pages/legal/TermsPage.tsx
  - client/src/components/legal/LegalPageLayout.tsx
  - client/src/App.tsx
autonomous: false
requirements: [LEGAL-01]
must_haves:
  truths:
    - "GET /legal/privacy returns 200 and renders Privacy Policy content (not the 404 NotFound page)"
    - "GET /legal/terms returns 200 and renders Terms of Service content (not the 404 NotFound page)"
    - "Both routes are public — no AuthGuard wrapper, accessible to logged-out users (per D-02)"
    - "Both pages render with a simple readable layout (max-width prose container, no chat shell, header with Back-to-home link, last-updated date) — per D-02 + D-03"
    - "Privacy page explicitly covers all data practices listed in D-01: Google OAuth scope (email/profile), Neon Postgres (US region), Stripe billing PII, LLM providers (Gemini/Groq/OpenAI — note user prompts are sent), session cookies (httpOnly, 7-day TTL), data retention, deletion path"
    - "Both pages display 'DRAFT — for legal review' marker prominently per D-04 (not hidden) so future readers know the copy is not lawyer-reviewed"
  artifacts:
    - path: client/src/components/legal/LegalPageLayout.tsx
      provides: "Reusable layout for legal pages: white background, max-width prose container, header with brand mark + back link, last-updated date row, DRAFT marker banner. No chat shell, no sidebar."
      min_lines: 50
    - path: client/src/pages/legal/PrivacyPage.tsx
      provides: "Privacy Policy content covering all data practices from D-01. Uses LegalPageLayout. Plain JSX (h2, p, ul) — no markdown parser per D-03."
      min_lines: 120
    - path: client/src/pages/legal/TermsPage.tsx
      provides: "Terms of Service content (acceptance, acceptable use, IP, AI output disclaimer, billing, termination, warranty disclaimer, liability cap, governing law, contact). Uses LegalPageLayout."
      min_lines: 100
    - path: client/src/App.tsx
      provides: "Two new <Route> entries for /legal/privacy and /legal/terms in the Switch — placed BEFORE the catch-all NotFound route at line 112. Public (no AuthGuard wrapper)."
      contains: "/legal/privacy"
  key_links:
    - from: client/src/App.tsx
      to: client/src/pages/legal/PrivacyPage.tsx
      via: import + Route path /legal/privacy component PrivacyPage
      pattern: "PrivacyPage"
    - from: client/src/App.tsx
      to: client/src/pages/legal/TermsPage.tsx
      via: import + Route path /legal/terms component TermsPage
      pattern: "TermsPage"
    - from: client/src/pages/LandingPage.tsx (existing footer link at line 561)
      to: client/src/pages/legal/PrivacyPage.tsx (rendered via /legal/privacy route)
      via: anchor href /legal/privacy
      pattern: "/legal/privacy"
---

<objective>
Close LEGAL-01 by creating actual content at `/legal/privacy` and `/legal/terms` so the existing footer/login links (shipped in commit `3bbab4c`) stop landing on NotFound.

Purpose: The audit (Apr 28) found that the Privacy/Terms anchor links work but the routes are unregistered — clicking them lands on NotFound.tsx, which is an audit-failing trust signal for a public-facing app handling Google OAuth + Stripe PII. This plan ships AI-drafted DRAFT copy that (a) describes Hatchin's actual data practices accurately and (b) is clearly marked DRAFT per D-04 so the user knows to circulate to a lawyer before final.

Output: Two new public route components (PrivacyPage, TermsPage), one shared LegalPageLayout, and two new Route entries in App.tsx — all client-side; no server changes.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/35-production-hotfix-pass/35-CONTEXT.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/CLAUDE.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/App.tsx
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/pages/LandingPage.tsx
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/pages/login.tsx

<interfaces>
<!-- Use these contracts directly. -->

App.tsx Switch (existing — line 64-114) lists Routes for /landing, /login, /, /maya/:projectId, /account, then catch-all NotFound. New legal Routes go BEFORE the catch-all. Wouter Route shape: same as the /login line — `<Route path="/legal/privacy" component={PrivacyPage} />`. Per D-02 these are PUBLIC, NO AuthGuard wrapping.

CLAUDE.md § 7 prompt rules apply to AI-drafted user-facing copy: human, colleague-tone — but legal copy is the exception. Use clear, plain-English legal copy (not corporate-stiff, not chat-casual). Lists/headings ARE acceptable in legal copy (overrides § 7 chat-tone rules).

Hatchin's actual data practices (from CLAUDE.md and STATE.md to be reflected accurately in the Privacy page):
- **Auth:** Google OAuth 2.0 + PKCE; scope is email + profile (sub, email, name, avatar_url) per CLAUDE.md § 8
- **Database:** Neon PostgreSQL (US region), 7-day session TTL, httpOnly+secure cookies (CLAUDE.md § 8)
- **LLM providers:** Gemini (primary, US) / OpenAI (fallback, US) / Groq (free-tier path, US). User chat content is sent to these providers as prompts. (CLAUDE.md § 1, § 7)
- **Billing:** Stripe Checkout for Pro tier ($19/mo), customer ID + subscription status stored in user row; full PII (card details) live with Stripe, not Hatchin (CLAUDE.md § 5 billing routes)
- **Brain documents:** PDF/DOCX/TXT/MD uploads stored in DB; 10MB max (CLAUDE.md § 0 v1.3)
- **Retention:** Session cookies 7 days. Account/project data persists until user-initiated deletion (no automatic data retention policy yet — note as such).
- **Deletion path:** Currently via support email (no self-service delete UI yet — note as such, contact email).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LegalPageLayout shared component + register routes in App.tsx</name>
  <files>client/src/components/legal/LegalPageLayout.tsx, client/src/App.tsx</files>
  <behavior>
    - LegalPageLayout receives props { title: string; lastUpdated: string; children: React.ReactNode } and renders:
      - White background page (bg-white text-slate-900 min-h-screen)
      - Top header bar with Hatchin brand mark + "Back to home" link to /
      - DRAFT banner (yellow/amber background) at top: "DRAFT — for legal review. Final copy under attorney review."
      - max-width prose container (max-w-3xl mx-auto px-6 py-12)
      - Title as h1 + subtitle "Last updated: {lastUpdated}"
      - children render below
    - App.tsx: import PrivacyPage + TermsPage; add two Route entries with paths /legal/privacy and /legal/terms BEFORE the catch-all NotFound route at line 112. Public — NO AuthGuard wrapper.
  </behavior>
  <action>
    Per D-02 (public routes, simple readable layout, no chat shell), D-03 (plain JSX content, no markdown parser), D-04 (DRAFT marker, last-updated date):

    1. **Create directory** client/src/components/legal/ if not present (Write tool creates parent dirs automatically).

    2. **LegalPageLayout.tsx**: Functional React component, plain Tailwind classes (no shadcn primitives needed — keep dependencies minimal so the page renders even if logged-out user has no auth state). Mirror the simple style of the login page header (an anchor pointing back to /, with the Hatchin brand mark).

       Light mode for legal pages (white background) per D-02 — use explicit Tailwind classes (bg-white text-slate-900) rather than relying on theme tokens, because legal pages should look identical regardless of user's theme preference.

       DRAFT banner: a div with role="note" using amber background classes (bg-amber-50 border border-amber-200 text-amber-900 px-4 py-3 text-sm) near the top with text "DRAFT — for legal review. Final copy under attorney review." Make this prominent (not collapsible).

    3. **Update client/src/App.tsx**:
       - Add imports near top: `import PrivacyPage from "@/pages/legal/PrivacyPage";` and `import TermsPage from "@/pages/legal/TermsPage";`
       - In the Switch block, BEFORE the NotFound catch-all line (currently line 112), add two new Route entries with paths /legal/privacy and /legal/terms pointing at PrivacyPage and TermsPage respectively.
       - Do NOT wrap in AuthGuard — D-02 explicitly mandates public routes.
       - The existing /landing and /login routes prove the public-route pattern (line 65-66) — follow that shape exactly.

    4. **No changes to existing footer/login links** — they already point to /legal/privacy and /legal/terms (verified in 35-CONTEXT.md canonical_refs at LandingPage.tsx:561,563 and login.tsx:86,88).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && grep -c "PrivacyPage\|TermsPage" client/src/App.tsx | awk '$1 >= 4 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    LegalPageLayout.tsx exists; App.tsx has both new Route entries before the NotFound catch-all; no AuthGuard wrapping; typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author PrivacyPage.tsx content covering all D-01 data practices</name>
  <files>client/src/pages/legal/PrivacyPage.tsx</files>
  <behavior>
    Page must contain sections (h2 headings) covering — and these strings should be searchable verbatim by 35-05's spec:
    - "Information We Collect" — Google OAuth (email, profile), session cookies, chat content
    - "How We Use Your Information" — operate the service, generate AI responses, billing
    - "Data Processors and Third Parties" — Google (OAuth identity verification), Neon (PostgreSQL hosting, US region), Stripe (billing PII), Gemini (Google) + OpenAI + Groq (LLM providers — receive your prompts as input)
    - "Cookies" — session cookie httpOnly + secure, 7-day TTL
    - "Data Retention" — account data persists until deletion request; no automatic retention policy yet
    - "Your Rights" — request deletion via the contact email below
    - "Changes to This Policy" — last-updated date governs; material changes notified in-app
    - "Contact" — hello@hatchin.ai
    - DRAFT marker visible (rendered via LegalPageLayout)
  </behavior>
  <action>
    Per D-01 (explicit coverage required), D-03 (plain JSX, no markdown parser), D-04 (DRAFT, last-updated):

    1. Create client/src/pages/legal/PrivacyPage.tsx. Default-export a functional component that wraps content in LegalPageLayout with title="Privacy Policy" and lastUpdated="2026-04-28".

    2. Content structure (use plain h2, p, ul, li — NO markdown parser). Each section covers a bullet from the behavior list above. Keep sentences direct and unambiguous; avoid corporate filler ("We value your privacy" boilerplate). Sample tone:

       > "We collect the email and name from your Google account when you sign in. We store this in our PostgreSQL database hosted by Neon (US region) so we can recognize you on return visits."

       Be explicit about LLM data flow: "When you send a message in chat, we send it to one of our LLM providers (Gemini by Google, OpenAI, or Groq depending on routing rules) so they can generate a reply. These providers process your prompt under their own privacy terms; they may retain inputs for abuse-detection per their published policies."

       Be explicit about Stripe: "If you upgrade to Pro, billing details (card numbers, billing address) are collected and stored by Stripe — not by Hatchin. We store only the Stripe customer ID and subscription status in our database."

       Be explicit about deletion: "To delete your account and all associated data, email hello@hatchin.ai. Self-service deletion is on our roadmap."

    3. **Tailwind prose classes**: Use a wrapper div with className "prose prose-slate max-w-none" OR use explicit per-element classes (h2 mt-8 text-2xl font-semibold etc.) — the prose plugin may or may not be installed. Verify by checking tailwind.config.ts for `@tailwindcss/typography` plugin; if absent, use explicit per-element classes. Either approach is acceptable per D-02 (simple readable doc layout).

    4. **Date string**: Use lastUpdated="2026-04-28" (today's date per environment context).

    5. **Word count**: Aim for 600-1000 words total — long enough to actually cover the practices, short enough to read.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && for token in Google Neon Stripe Gemini OpenAI Groq httpOnly DRAFT deletion; do grep -q "$token" client/src/pages/legal/PrivacyPage.tsx || { echo "MISSING: $token"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>
    PrivacyPage.tsx exists; renders via LegalPageLayout; all 9 required tokens (Google, Neon, Stripe, Gemini, OpenAI, Groq, httpOnly, DRAFT, deletion) present in source; typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author TermsPage.tsx content</name>
  <files>client/src/pages/legal/TermsPage.tsx</files>
  <behavior>
    Page contains sections (h2 headings):
    - "Acceptance of Terms" — by using Hatchin you agree to these terms
    - "Account Responsibilities" — keep credentials secure; you're responsible for activity on your account
    - "Acceptable Use" — no abuse, no scraping, no reverse-engineering, no using outputs to train competing models
    - "Intellectual Property" — content you create with Hatchin is yours; Hatchin retains rights to its software/branding
    - "AI-Generated Output" — outputs are AI-generated and may be inaccurate; verify before relying on them; Hatchin makes no warranty of fitness
    - "Subscription and Billing" — Pro tier monthly/annual; cancel anytime via Stripe customer portal
    - "Termination" — either party may terminate at any time
    - "Disclaimer of Warranties" — service provided as-is
    - "Limitation of Liability" — capped at amounts paid in past 12 months
    - "Governing Law" — placeholder ("California, USA" or per user's actual jurisdiction — leave a TODO comment for the lawyer review pass)
    - "Contact" — hello@hatchin.ai
    - DRAFT marker visible (rendered via LegalPageLayout)
  </behavior>
  <action>
    Per D-01 + D-04 + § 16 of CLAUDE.md (security checklist concerns map to acceptable-use clauses):

    1. Create client/src/pages/legal/TermsPage.tsx. Default-export a functional component that wraps content in LegalPageLayout with title="Terms of Service" and lastUpdated="2026-04-28".

    2. Content structure: 11 sections matching the behavior bullets. Plain JSX with h2, p, ul, li.

    3. Tone: clear, direct, plain-English legal copy. Sample:

       > "Hatchin generates content using AI models. Outputs are AI-generated and may contain inaccuracies. You should verify any output before relying on it for decisions that matter — Hatchin makes no warranty of fitness for any particular purpose."

    4. **Governing Law placeholder**: include a JSX comment of the form `{/* TODO: lawyer-review confirm governing law jurisdiction */}` plus a sentence like "These terms are governed by the laws of California, USA." Note in the SUMMARY.md that this is a placeholder for lawyer review.

    5. **Word count**: Aim for 500-900 words.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && for token in "Acceptance" "Acceptable Use" "AI-Generated" "Limitation of Liability" "Governing Law" "DRAFT" "hello@hatchin.ai"; do grep -q "$token" client/src/pages/legal/TermsPage.tsx || { echo "MISSING: $token"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>
    TermsPage.tsx exists; renders via LegalPageLayout; all 7 required tokens (Acceptance, Acceptable Use, AI-Generated, Limitation of Liability, Governing Law, DRAFT, hello@hatchin.ai) present; typecheck green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Two new public-route legal pages at /legal/privacy and /legal/terms, both rendered via LegalPageLayout (white background, max-width prose, DRAFT banner, last-updated date). Routes registered in App.tsx before the NotFound catch-all, no AuthGuard wrapping.

    This checkpoint exists per the saved feedback rule "UI/UX changes need user approval" (referenced in 35-CONTEXT.md canonical_refs as `feedback_ui_change_protocol.md`) AND because legal copy must be human-reviewed before merging per D-04.
  </what-built>
  <how-to-verify>
    1. `npm run dev` — start the dev server on port 5001 if not already running.
    2. Open http://localhost:5001/legal/privacy in a browser (logged out is fine — page is public).
       - Page loads with white background, NOT 404.
       - DRAFT banner visible at top in amber.
       - All 8 required sections present (Information We Collect, How We Use, Data Processors, Cookies, Data Retention, Your Rights, Changes, Contact).
       - Read the actual copy — does it accurately describe Hatchin's practices? Anything missing or factually wrong (esp. LLM provider list, Stripe relationship, Neon hosting region)?
    3. Open http://localhost:5001/legal/terms.
       - Page loads, DRAFT banner present, all 11 sections present.
       - Governing Law section has placeholder + TODO comment.
    4. Click "Back to home" link — returns to /.
    5. From the landing page footer, click Privacy and Terms — both navigate to the new pages (no longer 404).
    6. From /login, scroll to the bottom — Privacy and Terms links work.

    EXPECTED USER ACTION: Read both pages carefully and either (a) approve the copy as-is for the DRAFT ship per D-04, or (b) request specific edits before 35-05 spec runs.
  </how-to-verify>
  <resume-signal>Type "approved" once you've read both pages and they accurately describe Hatchin's data practices (DRAFT for lawyer-review is fine), OR list specific edits needed before 35-05 spec runs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → /legal/privacy | Page is public. No auth, no PII collected. Only risk: legal copy inaccuracy harms users / creates liability if they rely on it |
| AI-drafted legal copy → published page | LLM may hallucinate practices Hatchin doesn't actually do, or omit ones it does (e.g., a forgotten data processor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13 | Information Disclosure | Privacy/Terms copy accuracy | mitigate | DRAFT marker prominent on both pages (Task 1 LegalPageLayout DRAFT banner; D-04 mandates "DRAFT — for legal review" so users know not to rely on the copy as final). Last-updated date prevents stale-copy claims. Lawyer review happens out-of-band post-Phase-35. |
| T-35-14 | Repudiation | Lack of explicit data-processor list | mitigate | Privacy "Data Processors and Third Parties" section enumerates all processors (Google, Neon, Stripe, Gemini, OpenAI, Groq) per D-01. Spec verifies all 6 names appear in source. Future processors added via direct edit + lastUpdated bump. |
| T-35-15 | Tampering | Unauthenticated route exposure | accept | Pages are intentionally public per D-02 (logged-out users must read before signing up). No PII or session data on these pages. |
| T-35-16 | Denial of Service | Static legal pages — minimal attack surface | accept | Pure render, no DB queries, no API calls. CDN-cacheable in production. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Both pages render at their routes (manual check from Task 4 checkpoint OR automated GET in 35-05 spec)
3. All required tokens present in source files (verified by Task 2 + Task 3 grep checks)
4. App.tsx Switch ordering: legal routes before NotFound catch-all (verified by reading App.tsx after edit)
5. Footer links from LandingPage.tsx and login.tsx successfully reach the new pages (no longer NotFound)
</verification>

<success_criteria>
- LEGAL-01 ✓: /legal/privacy and /legal/terms render real content (not 404)
- D-01 ✓: Privacy page covers all 7 listed data practices (Google OAuth, Neon, Stripe, LLM providers with prompt-flow note, session cookies httpOnly+TTL, retention, deletion)
- D-02 ✓: Public routes (no AuthGuard), simple readable doc layout (no chat shell)
- D-03 ✓: Plain JSX content, no markdown parser, two separate component files
- D-04 ✓: DRAFT marker visible, last-updated date present, copy clearly marked for lawyer review
- Footer/login links from prior commits now resolve correctly (smoke-tested in checkpoint, automated in 35-05)
</success_criteria>

<output>
After completion, create `.planning/phases/35-production-hotfix-pass/35-04-SUMMARY.md` recording: route registration line numbers in App.tsx, LegalPageLayout API, and the lawyer-review TODO list (governing law jurisdiction confirmation + any user-requested copy edits from the checkpoint).
</output>
