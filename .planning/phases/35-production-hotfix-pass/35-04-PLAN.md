---
phase: 35-production-hotfix-pass
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - client/src/components/legal/PrivacyContent.tsx
  - client/src/components/legal/TermsContent.tsx
  - client/src/components/legal/LegalModal.tsx
  - client/src/components/legal/LegalPageLayout.tsx
  - client/src/pages/legal/PrivacyPage.tsx
  - client/src/pages/legal/TermsPage.tsx
  - client/src/App.tsx
  - client/src/pages/LandingPage.tsx
  - client/src/pages/login.tsx
autonomous: false
requirements: [LEGAL-01]
must_haves:
  truths:
    - "Clicking Privacy or Terms links in landing footer or login page opens a branded shadcn Dialog (modal) overlaying the current page — no navigation, no page reload (HYBRID-MODAL-DEFAULT per CONTEXT.md D-02 revision)"
    - "GET /legal/privacy ALSO returns 200 and renders the same Privacy Policy content full-page (not the 404 NotFound page) — for crawlers, deep-links, and partner/Stripe compliance asks (HYBRID-DEEPLINK-FALLBACK)"
    - "GET /legal/terms ALSO returns 200 and renders the same Terms of Service content full-page"
    - "Modal and full-page render the SAME content components (PrivacyContent, TermsContent) — single source of legal copy, zero duplication"
    - "Both routes are public — no AuthGuard wrapper, accessible to logged-out users (per D-02)"
    - "Modal uses shadcn Dialog with focus-trap, escape-to-close, scroll-on-overflow for long content; matches landing page brand colors (per user instruction 2026-05-04)"
    - "Footer/login Privacy + Terms links preserve `href=/legal/privacy` and `href=/legal/terms` so middle-click and right-click 'Open in new tab' still navigate to the standalone page; left-click is intercepted with preventDefault to open modal instead"
    - "Privacy content explicitly covers all data practices listed in D-01 (with the post-D-01 LLM-stack revision per Phase A): Google OAuth scope (email/profile), Neon Postgres (US region), Stripe billing PII, LLM providers (DeepSeek primary + China-hosted, Gemini hot fallback US, Groq free-tier US — note user prompts are sent and DeepSeek's China hosting must be disclosed), session cookies (httpOnly, 7-day TTL), data retention, deletion path"
    - "Both pages and modal display 'DRAFT — for legal review' marker prominently per D-04 (not hidden) so future readers know the copy is not lawyer-reviewed"
  artifacts:
    - path: client/src/components/legal/PrivacyContent.tsx
      provides: "Pure JSX content component for Privacy Policy — h2/p/ul structure covering all 8 sections (Information We Collect, How We Use, Data Processors, Cookies, Data Retention, Your Rights, Changes, Contact). No layout chrome — just the content. Imported by both LegalModal (when type='privacy') and PrivacyPage. Plain JSX, no markdown parser per D-03."
      min_lines: 100
    - path: client/src/components/legal/TermsContent.tsx
      provides: "Pure JSX content component for Terms of Service — h2/p/ul structure covering 11 sections (Acceptance, Acceptable Use, Account, Subscription/Billing, IP, AI Output Disclaimer, Termination, Warranty Disclaimer, Liability Cap, Governing Law, Contact). Imported by both LegalModal (when type='terms') and TermsPage."
      min_lines: 90
    - path: client/src/components/legal/LegalModal.tsx
      provides: "Branded shadcn Dialog wrapping PrivacyContent or TermsContent based on `type` prop. Props: { open: boolean; onOpenChange: (open: boolean) => void; type: 'privacy' | 'terms' }. Dialog max-w-3xl, max-h 80vh with internal scroll, DialogTitle = 'Privacy Policy' | 'Terms of Service', DialogDescription = lastUpdated date + DRAFT marker. Brand-matching: dark hero gradient on landing-context (matches LandingPage palette), default light card on login-context — auto-detected via theme or explicit prop. Focus-trap + escape-to-close come free from Radix Dialog primitive."
      min_lines: 60
    - path: client/src/components/legal/LegalPageLayout.tsx
      provides: "Reusable layout for STANDALONE legal-page route renders only (deep-link fallback): white background, max-width prose container, header with brand mark + back-to-home link, last-updated date row, DRAFT marker banner. No chat shell, no sidebar. NOT used by LegalModal — modal has its own DialogContent chrome."
      min_lines: 50
    - path: client/src/pages/legal/PrivacyPage.tsx
      provides: "Thin wrapper rendering <LegalPageLayout title='Privacy Policy' lastUpdated='2026-05-04'><PrivacyContent /></LegalPageLayout>. Mounted at /legal/privacy route. Used when user navigates directly (deep-link, middle-click new-tab, etc.) rather than clicking the footer link."
      min_lines: 12
    - path: client/src/pages/legal/TermsPage.tsx
      provides: "Thin wrapper rendering <LegalPageLayout title='Terms of Service' lastUpdated='2026-05-04'><TermsContent /></LegalPageLayout>. Mounted at /legal/terms route."
      min_lines: 12
    - path: client/src/App.tsx
      provides: "Two new <Route> entries for /legal/privacy and /legal/terms in the Switch — placed BEFORE the catch-all NotFound route at line 112. Public (no AuthGuard wrapper). These power the deep-link fallback half of the hybrid."
      contains: "/legal/privacy"
    - path: client/src/pages/LandingPage.tsx
      provides: "Footer Privacy + Terms anchor links at lines 561, 563 modified: anchor `href` preserved (so middle-click / right-click 'Open in new tab' still navigates to standalone page). Left-click intercepted with `e.preventDefault()` and onClick handler opens LegalModal with the appropriate type. Uses local React state to control modal open/close."
      contains: "LegalModal"
    - path: client/src/pages/login.tsx
      provides: "Footer Privacy + Terms anchor links at lines 86, 88 modified: same hybrid pattern as LandingPage. Anchor href preserved + left-click opens LegalModal."
      contains: "LegalModal"
  key_links:
    - from: client/src/pages/LandingPage.tsx (footer line 561 + 563)
      to: client/src/components/legal/LegalModal.tsx (modal opens on click)
      via: onClick handler with preventDefault + setOpen state
      pattern: "LegalModal"
    - from: client/src/pages/login.tsx (line 86 + 88)
      to: client/src/components/legal/LegalModal.tsx (same pattern as landing footer)
      via: onClick handler with preventDefault + setOpen state
      pattern: "LegalModal"
    - from: client/src/components/legal/LegalModal.tsx
      to: client/src/components/legal/PrivacyContent.tsx + TermsContent.tsx
      via: imports + conditional render based on `type` prop
      pattern: "type === 'privacy' ? <PrivacyContent /> : <TermsContent />"
    - from: client/src/App.tsx
      to: client/src/pages/legal/PrivacyPage.tsx + TermsPage.tsx (deep-link fallback)
      via: import + Route path /legal/privacy component PrivacyPage (and equivalent for terms)
      pattern: "PrivacyPage"
    - from: client/src/pages/legal/PrivacyPage.tsx + TermsPage.tsx
      to: client/src/components/legal/PrivacyContent.tsx + TermsContent.tsx
      via: page wraps shared content in LegalPageLayout
      pattern: "PrivacyContent"
---

<objective>
Close LEGAL-01 by creating actual content at `/legal/privacy` and `/legal/terms` so the existing footer/login links (shipped in commit `3bbab4c`) stop landing on NotFound.

Purpose: The audit (Apr 28) found that the Privacy/Terms anchor links work but the routes are unregistered — clicking them lands on NotFound.tsx, which is an audit-failing trust signal for a public-facing app handling Google OAuth + Stripe PII. This plan ships AI-drafted DRAFT copy that (a) describes Hatchin's actual data practices accurately and (b) is clearly marked DRAFT per D-04 so the user knows to circulate to a lawyer before final.

Output: Two new public route components (PrivacyPage, TermsPage), one shared LegalPageLayout, and two new Route entries in App.tsx — all client-side; no server changes.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/35-production-hotfix-pass/35-CONTEXT.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/CLAUDE.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/App.tsx
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/pages/LandingPage.tsx
@/Users/shashankrai/Documents/hatching-mvp-5th-march/client/src/pages/login.tsx

<interfaces>
<!-- Use these contracts directly. -->

App.tsx Switch (existing — line 64-114) lists Routes for /landing, /login, /, /maya/:projectId, /account, then catch-all NotFound. New legal Routes go BEFORE the catch-all. Wouter Route shape: same as the /login line — `<Route path="/legal/privacy" component={PrivacyPage} />`. Per D-02 these are PUBLIC, NO AuthGuard wrapping.

CLAUDE.md § 7 prompt rules apply to AI-drafted user-facing copy: human, colleague-tone — but legal copy is the exception. Use clear, plain-English legal copy (not corporate-stiff, not chat-casual). Lists/headings ARE acceptable in legal copy (overrides § 7 chat-tone rules).

Hatchin's actual data practices (from CLAUDE.md and STATE.md to be reflected accurately in the Privacy page):
- **Auth:** Google OAuth 2.0 + PKCE; scope is email + profile (sub, email, name, avatar_url) per CLAUDE.md § 8
- **Database:** Neon PostgreSQL (US region), 7-day session TTL, httpOnly+secure cookies (CLAUDE.md § 8)
- **LLM providers (per commit 34c8f23 + DeepSeek migration):** DeepSeek V4-Flash/Pro (primary, **China-hosted**) / Gemini 2.5-Flash/Pro by Google (hot fallback, US) / Groq Llama 3.3-70B (free-tier path for simple chat / task extraction / compaction, US). OpenAI removed from default prod chain (escape hatch only via `LLM_PRIMARY=openai`). User chat content is sent to whichever provider in the chain successfully accepts the request, as prompts. DeepSeek's China hosting is a meaningful data-residency disclosure and MUST be called out explicitly in the privacy page.
- **Billing:** Stripe Checkout for Pro tier ($19/mo), customer ID + subscription status stored in user row; full PII (card details) live with Stripe, not Hatchin (CLAUDE.md § 5 billing routes)
- **Brain documents:** PDF/DOCX/TXT/MD uploads stored in DB; 10MB max (CLAUDE.md § 0 v1.3)
- **Retention:** Session cookies 7 days. Account/project data persists until user-initiated deletion (no automatic data retention policy yet — note as such).
- **Deletion path:** Currently via support email (no self-service delete UI yet — note as such, contact email).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LegalPageLayout shared component (used by deep-link page mode only)</name>
  <files>client/src/components/legal/LegalPageLayout.tsx</files>
  <behavior>
    - LegalPageLayout receives props { title: string; lastUpdated: string; children: React.ReactNode } and renders the STANDALONE-PAGE chrome only (used when user navigates directly to /legal/privacy or /legal/terms; NOT used by LegalModal which has its own DialogContent chrome):
      - White background page (bg-white text-slate-900 min-h-screen)
      - Top header bar with Hatchin brand mark + "Back to home" link to /
      - DRAFT banner (yellow/amber background): "DRAFT — for legal review. Final copy under attorney review."
      - max-width prose container (max-w-3xl mx-auto px-6 py-12)
      - Title as h1 + subtitle "Last updated: {lastUpdated}"
      - children render below
  </behavior>
  <action>
    Per D-02 (public routes when navigated directly, simple readable layout, no chat shell), D-03 (plain JSX content), D-04 (DRAFT marker, last-updated date):

    1. **Create directory** client/src/components/legal/ if not present (Write tool creates parent dirs automatically).

    2. **LegalPageLayout.tsx**: Functional React component, plain Tailwind classes (no shadcn primitives needed). Mirror the simple style of the login page header (an anchor pointing back to /, with the Hatchin brand mark).

       Light mode for legal pages (white background) per D-02 — use explicit Tailwind classes (bg-white text-slate-900) rather than relying on theme tokens, because legal pages should look identical regardless of user's theme preference.

       DRAFT banner: a div with role="note" using amber background classes (bg-amber-50 border border-amber-200 text-amber-900 px-4 py-3 text-sm) near the top with text "DRAFT — for legal review. Final copy under attorney review." Make this prominent (not collapsible).

    3. **No App.tsx changes in this task** — route registration moves to Task 5 (which also creates the page-wrappers themselves).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && test -f client/src/components/legal/LegalPageLayout.tsx && grep -q "DRAFT" client/src/components/legal/LegalPageLayout.tsx</automated>
  </verify>
  <done>
    LegalPageLayout.tsx exists with bg-white text-slate-900 + DRAFT banner + max-width prose container + Back-to-home link. Used by Task 5's page wrappers only.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author PrivacyContent.tsx — shared content used by both modal and page</name>
  <files>client/src/components/legal/PrivacyContent.tsx</files>
  <behavior>
    Pure content component — receives no props, renders ONLY the body sections (h2 + p + ul). NO outer chrome (no max-width container, no DRAFT banner, no title heading — those belong to LegalModal or LegalPageLayout). Sections (h2 headings, searchable verbatim by 35-05's spec):
    - "Information We Collect" — Google OAuth (email, profile), session cookies, chat content
    - "How We Use Your Information" — operate the service, generate AI responses, billing
    - "Data Processors and Third Parties" — Google (OAuth identity verification), Neon (PostgreSQL hosting, US region), Stripe (billing PII), DeepSeek (LLM primary, **China-hosted** — receives your prompts as input) + Gemini by Google (LLM hot fallback, US) + Groq (LLM free-tier path for routing-eligible workloads, US). Disclose DeepSeek's China hosting explicitly so users with EU/regulated data concerns can opt out by emailing support before sending sensitive content.
    - "Cookies" — session cookie httpOnly + secure, 7-day TTL
    - "Data Retention" — account data persists until deletion request; no automatic retention policy yet
    - "Your Rights" — request deletion via the contact email below
    - "Changes to This Policy" — last-updated date governs; material changes notified in-app
    - "Contact" — hello@hatchin.ai
  </behavior>
  <action>
    Per D-01 (explicit coverage required), D-03 (plain JSX, no markdown parser), D-04 (DRAFT, last-updated — DRAFT is rendered by parent, not here):

    1. Create client/src/components/legal/PrivacyContent.tsx. Default-export a functional component with no props, renders a fragment or div containing the 8 sections.

    2. Content structure (use plain h2, p, ul, li — NO markdown parser). Sections cover bullets from the behavior list. Keep sentences direct; avoid corporate filler. Sample tone:

       > "We collect the email and name from your Google account when you sign in. We store this in our PostgreSQL database hosted by Neon (US region) so we can recognize you on return visits."

       Be explicit about LLM data flow AND about DeepSeek's China hosting (the most material disclosure for any EU/regulated reader): "When you send a message in chat, we route it to one of our LLM providers depending on the workload. Our primary provider is DeepSeek (hosted in China) — most chat messages and autonomous work go there. If DeepSeek is unavailable or for specific workloads (simple chat, task extraction, conversation compaction), we route to Gemini (Google, US) or Groq (US). These providers process your prompt under their own privacy terms and may retain inputs for abuse-detection per their published policies. If you do not want your content sent to a China-hosted provider, contact us at hello@hatchin.ai before sending sensitive material — per-customer routing overrides are available on request."

       Be explicit about Stripe: "If you upgrade to Pro, billing details (card numbers, billing address) are collected and stored by Stripe — not by Hatchin. We store only the Stripe customer ID and subscription status in our database."

       Be explicit about deletion: "To delete your account and all associated data, email hello@hatchin.ai. Self-service deletion is on our roadmap."

    3. **Tailwind classes**: Use explicit per-element classes (h2 mt-8 text-2xl font-semibold; p mt-3 text-base leading-relaxed; ul mt-3 list-disc list-inside space-y-1) so content renders correctly without depending on `@tailwindcss/typography` plugin. The component must look correct in both modal (max-h scroll) and full-page (max-w-3xl) contexts.

    4. **Word count**: Aim for 600-1000 words total — long enough to cover practices, short enough to read in a modal.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && for token in Google Neon Stripe DeepSeek Gemini Groq China httpOnly deletion hello@hatchin.ai; do grep -q "$token" client/src/components/legal/PrivacyContent.tsx || { echo "MISSING: $token"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>
    PrivacyContent.tsx exists as a chrome-less shared content component; all 10 required tokens present (Google, Neon, Stripe, DeepSeek, Gemini, Groq, China, httpOnly, deletion, hello@hatchin.ai); typecheck green. Will be imported by both LegalModal (Task 4) and PrivacyPage (Task 5).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author TermsContent.tsx — shared content used by both modal and page</name>
  <files>client/src/components/legal/TermsContent.tsx</files>
  <behavior>
    Pure content component (same structure as PrivacyContent) — no props, no outer chrome. Sections (h2 headings):
    - "Acceptance of Terms" — by using Hatchin you agree to these terms
    - "Account Responsibilities" — keep credentials secure; you're responsible for activity on your account
    - "Acceptable Use" — no abuse, no scraping, no reverse-engineering, no using outputs to train competing models
    - "Intellectual Property" — content you create with Hatchin is yours; Hatchin retains rights to its software/branding
    - "AI-Generated Output" — outputs are AI-generated and may be inaccurate; verify before relying on them; Hatchin makes no warranty of fitness
    - "Subscription and Billing" — Pro tier monthly/annual; cancel anytime via Stripe customer portal
    - "Termination" — either party may terminate at any time
    - "Disclaimer of Warranties" — service provided as-is
    - "Limitation of Liability" — capped at amounts paid in past 12 months
    - "Governing Law" — placeholder ("California, USA" or per user's actual jurisdiction — leave a TODO comment for the lawyer review pass)
    - "Contact" — hello@hatchin.ai
  </behavior>
  <action>
    Per D-01 + D-04 + § 16 of CLAUDE.md:

    1. Create client/src/components/legal/TermsContent.tsx. Default-export a functional component with no props.

    2. Content structure: 11 sections matching the behavior bullets. Plain JSX with h2, p, ul, li. Same Tailwind class pattern as PrivacyContent (explicit per-element classes).

    3. Tone: clear, direct, plain-English legal copy. Sample:

       > "Hatchin generates content using AI models. Outputs are AI-generated and may contain inaccuracies. You should verify any output before relying on it for decisions that matter — Hatchin makes no warranty of fitness for any particular purpose."

    4. **Governing Law placeholder**: include a JSX comment of the form `{/* TODO: lawyer-review confirm governing law jurisdiction */}` plus a sentence like "These terms are governed by the laws of California, USA." Note in the SUMMARY.md that this is a placeholder for lawyer review.

    5. **Word count**: Aim for 500-900 words.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && for token in "Acceptance" "Acceptable Use" "AI-Generated" "Limitation of Liability" "Governing Law" "hello@hatchin.ai"; do grep -q "$token" client/src/components/legal/TermsContent.tsx || { echo "MISSING: $token"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>
    TermsContent.tsx exists as a chrome-less shared content component; all 6 required tokens (Acceptance, Acceptable Use, AI-Generated, Limitation of Liability, Governing Law, hello@hatchin.ai) present; typecheck green. Will be imported by LegalModal (Task 4) and TermsPage (Task 5).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create LegalModal — branded shadcn Dialog wrapping shared content</name>
  <files>client/src/components/legal/LegalModal.tsx</files>
  <behavior>
    Functional component with props { open: boolean; onOpenChange: (open: boolean) => void; type: 'privacy' | 'terms' }. Renders a shadcn Dialog (already exists at client/src/components/ui/dialog.tsx, uses Radix Dialog primitive — focus-trap and escape-to-close are free). DialogContent uses max-w-3xl, max-h-[80vh], overflow-y-auto on the body region. Header shows DialogTitle = 'Privacy Policy' or 'Terms of Service'. Description row: "DRAFT — for legal review · Last updated 2026-05-04" (amber accent text matching the LegalPageLayout DRAFT banner). Body conditionally renders <PrivacyContent /> when type='privacy' or <TermsContent /> when type='terms'. Close button (DialogClose) in header; clicking outside also dismisses (Radix default).

    Brand-matching: modal renders against whatever page triggered it. Use neutral DialogContent styling (bg-background text-foreground per shadcn defaults) — this matches both the dark gradient landing AND the light login page acceptably without context-detection logic. If the user wants stronger contextual styling later, that's a polish phase.
  </behavior>
  <action>
    1. Create client/src/components/legal/LegalModal.tsx.

    2. Imports: React, the shadcn Dialog primitives from "@/components/ui/dialog" (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription), PrivacyContent from "./PrivacyContent", TermsContent from "./TermsContent".

    3. Props type: `interface LegalModalProps { open: boolean; onOpenChange: (open: boolean) => void; type: 'privacy' | 'terms'; }`. Default-export the component.

    4. Render structure:
       ```tsx
       <Dialog open={open} onOpenChange={onOpenChange}>
         <DialogContent className="max-w-3xl max-h-[80vh] flex flex-col p-0">
           <DialogHeader className="px-6 pt-6 pb-4 border-b">
             <DialogTitle>{type === 'privacy' ? 'Privacy Policy' : 'Terms of Service'}</DialogTitle>
             <DialogDescription className="text-amber-600">
               DRAFT — for legal review · Last updated 2026-05-04
             </DialogDescription>
           </DialogHeader>
           <div className="overflow-y-auto px-6 py-4">
             {type === 'privacy' ? <PrivacyContent /> : <TermsContent />}
           </div>
         </DialogContent>
       </Dialog>
       ```

    5. Accessibility: Radix Dialog handles focus-trap, focus-restore-on-close, escape-to-close, and aria-labelledby/describedby automatically. Do NOT add a manual focus-trap. Do NOT remove the native Close button — keyboard users need Escape AND a visible close affordance.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && grep -q "PrivacyContent\|TermsContent" client/src/components/legal/LegalModal.tsx && grep -q "max-h" client/src/components/legal/LegalModal.tsx && grep -q "DRAFT" client/src/components/legal/LegalModal.tsx</automated>
  </verify>
  <done>
    LegalModal.tsx exists with shadcn Dialog wrapper, PrivacyContent + TermsContent imports, max-h scroll for long content, DRAFT marker in DialogDescription. Typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create thin PrivacyPage + TermsPage wrappers + register routes in App.tsx (deep-link fallback)</name>
  <files>client/src/pages/legal/PrivacyPage.tsx, client/src/pages/legal/TermsPage.tsx, client/src/App.tsx</files>
  <behavior>
    Two thin functional components that wrap shared content in LegalPageLayout (page-mode chrome). Each ~10-15 lines. App.tsx registers two new public Routes for /legal/privacy and /legal/terms — these are the deep-link fallback half of the hybrid (when a user navigates directly to the URL via crawler / shared link / middle-click new-tab, they get a real standalone page rather than the homepage with auto-popup).
  </behavior>
  <action>
    1. **PrivacyPage.tsx**:
       ```tsx
       import LegalPageLayout from "@/components/legal/LegalPageLayout";
       import PrivacyContent from "@/components/legal/PrivacyContent";
       export default function PrivacyPage() {
         return (
           <LegalPageLayout title="Privacy Policy" lastUpdated="2026-05-04">
             <PrivacyContent />
           </LegalPageLayout>
         );
       }
       ```

    2. **TermsPage.tsx**: same shape, title="Terms of Service", renders <TermsContent />.

    3. **App.tsx**:
       - Add imports: `import PrivacyPage from "@/pages/legal/PrivacyPage";` and `import TermsPage from "@/pages/legal/TermsPage";` near the existing AccountPage import (line 15).
       - In the Switch block, BEFORE the catch-all `<Route component={NotFound} />` at line 112, add:
         ```tsx
         <Route path="/legal/privacy" component={PrivacyPage} />
         <Route path="/legal/terms" component={TermsPage} />
         ```
       - Do NOT wrap in AuthGuard — these routes are public per D-02. Pattern matches existing /landing and /login public routes (line 65-66).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && grep -c "PrivacyPage\|TermsPage" client/src/App.tsx | awk '$1 >= 4 { exit 0 } { exit 1 }' && test -f client/src/pages/legal/PrivacyPage.tsx && test -f client/src/pages/legal/TermsPage.tsx</automated>
  </verify>
  <done>
    PrivacyPage.tsx + TermsPage.tsx exist as thin wrappers (≤15 lines each) around LegalPageLayout + shared content components. App.tsx has 2 new Route entries before the catch-all NotFound, no AuthGuard. Typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Wire footer + login Privacy/Terms anchors to open modal on click (preserve href fallback)</name>
  <files>client/src/pages/LandingPage.tsx, client/src/pages/login.tsx</files>
  <behavior>
    Existing anchors at LandingPage.tsx:561,563 and login.tsx:86,88 currently say `<a href="/legal/privacy">Privacy</a>` and `<a href="/legal/terms">Terms</a>`. After this task:
    - Anchor `href` attributes preserved unchanged (so middle-click / right-click "Open in new tab" / "Copy link" still works against the standalone deep-link routes from Task 5)
    - Left-click intercepted via onClick handler that calls `e.preventDefault()` and opens the LegalModal with the appropriate `type`
    - Each page (LandingPage, login) holds local React state for { modalOpen: boolean, modalType: 'privacy' | 'terms' }
    - LegalModal mounted at the bottom of each page's JSX tree, controlled by that local state
  </behavior>
  <action>
    Pattern (apply to BOTH LandingPage.tsx and login.tsx):

    1. **Add state** near the top of the component:
       ```tsx
       const [legalModal, setLegalModal] = useState<{ open: boolean; type: 'privacy' | 'terms' }>({ open: false, type: 'privacy' });
       ```

    2. **Modify the Privacy anchor** (lines 561 and 86):
       - BEFORE: `<a href="/legal/privacy" className="...">Privacy</a>`
       - AFTER: `<a href="/legal/privacy" onClick={(e) => { e.preventDefault(); setLegalModal({ open: true, type: 'privacy' }); }} className="...">Privacy</a>`
       - **Keep the className the same** — visual styling unchanged; only the click behavior changes.
       - **Keep the href** so middle-click new-tab and right-click "Open in new tab" / "Copy link" still navigate to /legal/privacy as the deep-link page.

    3. **Modify the Terms anchor** (lines 563 and 88): same pattern with type: 'terms' and href="/legal/terms".

    4. **Mount the modal** at the end of the component's JSX (just before the closing tag of the component's root element):
       ```tsx
       <LegalModal
         open={legalModal.open}
         onOpenChange={(open) => setLegalModal((prev) => ({ ...prev, open }))}
         type={legalModal.type}
       />
       ```

    5. **Imports**: add `import LegalModal from "@/components/legal/LegalModal";` and `import { useState } from "react";` (the latter likely already imported — check first to avoid duplicate).

    6. **Do NOT touch the existing visual styling, layout, or copy** — only behavior changes. UI/UX must look identical except that left-click now opens a modal instead of navigating.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && grep -q "LegalModal" client/src/pages/LandingPage.tsx && grep -q "LegalModal" client/src/pages/login.tsx && grep -q "preventDefault" client/src/pages/LandingPage.tsx && grep -q "preventDefault" client/src/pages/login.tsx</automated>
  </verify>
  <done>
    LandingPage.tsx and login.tsx both: (a) preserve their existing Privacy/Terms `href` anchors visually unchanged, (b) import LegalModal, (c) hold local modal state, (d) intercept left-click with preventDefault and open modal, (e) mount LegalModal at component bottom controlled by local state. Typecheck green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Hybrid legal-content rendering — modal on click, standalone page on deep-link:
    - Modal default: clicking Privacy or Terms in landing footer or login page opens a branded shadcn Dialog (LegalModal) with the same content, focus-trap, escape-to-close, and DRAFT marker
    - Deep-link fallback: navigating directly to /legal/privacy or /legal/terms (via URL bar, middle-click new-tab, right-click "Open in new tab", external links from crawlers/Stripe/partners) renders the same content full-page via LegalPageLayout
    - Single source of legal copy: PrivacyContent and TermsContent components are imported by both modal and page wrappers — zero duplication

    This checkpoint exists per the saved feedback rule "UI/UX changes need user approval" (referenced in 35-CONTEXT.md canonical_refs as `feedback_ui_change_protocol.md`) AND because legal copy must be human-reviewed before merging per D-04. Two paths to test independently — both must work.
  </what-built>
  <how-to-verify>
    **Modal path (default UX):**
    1. `npm run dev` — start the dev server on port 5001 if not already running.
    2. Open http://localhost:5001 — landing page loads.
    3. Scroll to footer. Left-click "Privacy" link.
       - Modal opens overlaying the page (does NOT navigate away — URL stays at /).
       - DialogTitle reads "Privacy Policy".
       - DialogDescription shows "DRAFT — for legal review · Last updated 2026-05-05" in amber.
       - All 8 required sections visible (scroll inside modal if needed).
       - Read the copy — does it accurately describe Hatchin's practices? Anything missing or factually wrong (esp. DeepSeek primary + China-hosted disclosure, Gemini hot fallback US, Groq US, OpenAI explicitly NOT in default chain, Stripe relationship, Neon US hosting)?
       - Press Escape — modal dismisses, focus returns to the Privacy link.
       - Click outside the modal (on the overlay) — modal dismisses.
    4. Click "Terms" in footer.
       - Modal opens with "Terms of Service" title.
       - All 11 sections present, including Governing Law placeholder.
    5. Open http://localhost:5001/login — login page loads.
    6. Scroll to footer. Click Privacy and Terms links there.
       - Modal opens in the login page context. Same content, same DRAFT marker.

    **Deep-link path (fallback UX):**
    7. Open http://localhost:5001/legal/privacy DIRECTLY in a browser.
       - Standalone page loads with white background, NOT 404.
       - LegalPageLayout chrome visible: top header with brand + "Back to home" link, amber DRAFT banner, max-width prose container, "Last updated 2026-05-05" subtitle.
       - Same 8 sections from the modal — content is identical.
    8. Open http://localhost:5001/legal/terms directly.
       - Standalone page loads, DRAFT banner present, all 11 sections present.
    9. Click "Back to home" — returns to /.

    **Mixed-mode interactions:**
    10. From landing footer, middle-click "Privacy" — opens /legal/privacy in a new tab as the standalone page (anchor href fallback works).
    11. From landing footer, right-click "Privacy" → "Copy link address" — copies http://localhost:5001/legal/privacy. Paste in another tab — standalone page loads.
    12. Visual brand-match check: modal styling matches the page it opens from acceptably (it's a neutral shadcn Dialog — no jarring color contrast).

    EXPECTED USER ACTION: Read the copy in BOTH the modal and the standalone-page renders. Approve as-is for the DRAFT ship per D-04, or list specific edits before 35-05 spec runs.

    Per saved `feedback_ui_change_protocol.md`: take screenshots of (a) the landing footer with Privacy link visible, (b) modal opened from landing footer, (c) modal opened from login footer, (d) standalone /legal/privacy page render. Show the user before requesting "approved".
  </how-to-verify>
  <resume-signal>Type "approved" once both modal and standalone-page paths render correctly AND the legal copy accurately describes Hatchin's data practices (DRAFT for lawyer-review is fine). OR list specific edits needed before 35-05 spec runs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → /legal/privacy + /legal/terms (deep-link) | Pages are public. No auth, no PII collected. Only risk: legal copy inaccuracy harms users / creates liability if they rely on it |
| public web → footer click → LegalModal | Modal renders client-side over current page. Same content as deep-link page. Risk: focus-trap/escape semantics must work for accessibility |
| AI-drafted legal copy → published surfaces (modal + page) | LLM may hallucinate practices Hatchin doesn't actually do, or omit ones it does (e.g., a forgotten data processor) — modal AND page render the SAME content from shared components, so a copy error appears in both |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13 | Information Disclosure | Privacy/Terms copy accuracy | mitigate | DRAFT marker prominent in BOTH modal (DialogDescription amber text) and page (LegalPageLayout amber banner); D-04 mandates "DRAFT — for legal review" so users know not to rely on the copy as final. Last-updated date prevents stale-copy claims. Lawyer review happens out-of-band post-Phase-35. Single-source content components mean a fix lands once and updates both surfaces. |
| T-35-14 | Repudiation | Lack of explicit data-processor list | mitigate | Privacy "Data Processors and Third Parties" section enumerates current default processors (Google, Neon, Stripe, DeepSeek, Gemini, Groq) per D-01 with the Phase A revision. PrivacyContent.tsx grep verifies all 6 names appear AND the word "China" appears (gates the DeepSeek hosting disclosure). OpenAI removed from default list per commit 34c8f23 (escape-hatch only). Future processor changes go via direct edit + lastUpdated bump. Both modal and page inherit changes automatically. |
| T-35-15 | Tampering | Unauthenticated route exposure | accept | Pages are intentionally public per D-02 (logged-out users must read before signing up). No PII or session data on these pages. Same applies to modal — no auth state leaks via Dialog content. |
| T-35-16 | Denial of Service | Static legal pages — minimal attack surface | accept | Pure render, no DB queries, no API calls. CDN-cacheable in production. Modal adds zero server load. |
| T-35-23 | Information Disclosure | Modal accessibility (focus-trap, escape, screen-reader) | mitigate | Use Radix Dialog primitive (already in client/src/components/ui/dialog.tsx) — provides focus-trap, focus-restore-on-close, Escape-to-close, aria-labelledby/describedby automatically. Do NOT add manual focus-trap. Do NOT remove the native DialogClose button. Verified at human-verify checkpoint by Escape-key dismissal test. |
| T-35-24 | Tampering | Anchor href stripping by extensions/scripts | accept | If an ad-blocker / browser extension strips the onClick handler, the anchor's href fallback navigates to the standalone /legal/privacy page — degrades gracefully to deep-link path, content still served. No data leak; user just gets the page-mode UX. |
| T-35-25 | Information Disclosure | Search-engine crawling needs deep-link | mitigate | Modal is JS-only and not crawlable. Deep-link routes (/legal/privacy, /legal/terms) ARE indexable by Google + crawler-equivalents. Stripe / partners / browser trust-signal scanners that crawl `legal/privacy` URLs hit the standalone page. Crawl-safety preserved by hybrid design — pure-modal would have failed this. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Modal path: clicking footer Privacy/Terms in landing AND login opens LegalModal with correct content (manual check from human-verify checkpoint, automated in 35-05 case 1+2)
3. Deep-link path: /legal/privacy and /legal/terms render standalone pages via LegalPageLayout (automated GET in 35-05 case 1b+2b)
4. Single content source: PrivacyContent + TermsContent are imported by both LegalModal AND PrivacyPage/TermsPage (verified by grep across the 4 consumers)
5. All required tokens present in shared content files (verified by Task 2 + Task 3 grep checks)
6. App.tsx Switch ordering: legal routes before NotFound catch-all (verified by reading App.tsx after edit)
7. Footer/login anchors preserve `href` for middle-click new-tab fallback (verified by grep + manual middle-click test in checkpoint)
</verification>

<success_criteria>
- LEGAL-01 ✓: Both modal-on-click AND deep-link /legal/privacy + /legal/terms render real content (not 404, not stub)
- D-01 ✓: PrivacyContent covers all 7 data practices with the post-D-01 LLM-stack revision per Phase A — Google OAuth, Neon, Stripe, LLM providers (DeepSeek primary China-hosted, Gemini hot fallback US, Groq US — OpenAI explicitly NOT in default chain) with prompt-flow note + DeepSeek-China-hosting disclosure, session cookies httpOnly+TTL, retention, deletion. Same content appears in both modal and standalone page.
- D-02 (revised per user instruction 2026-05-04 + 2026-05-05): Modal-default UX with deep-link page fallback. Public routes (no AuthGuard) on both modal and page. Simple readable layout (modal: DialogContent max-h scroll; page: LegalPageLayout max-w prose). No chat shell on either.
- D-03 ✓: Plain JSX content (no markdown parser), 4 component files (2 shared content, 1 modal, 1 page layout) + 2 thin page wrappers — single source of truth for legal copy
- D-04 ✓: DRAFT marker visible in both modal (DialogDescription amber) and page (LegalPageLayout amber banner), last-updated date present in both
- Hybrid integrity ✓: middle-click on footer Privacy → opens /legal/privacy as standalone page (anchor href fallback works); left-click → opens modal (preventDefault intercept); same content in both
- Crawl-safety ✓: deep-link routes are crawlable; meets Stripe / partner / browser-trust-signal expectations
</success_criteria>

<output>
After completion, create `.planning/phases/35-production-hotfix-pass/35-04-SUMMARY.md` recording: route registration line numbers in App.tsx, LegalModal + LegalPageLayout APIs, modal-vs-page consumer count, and the lawyer-review TODO list (governing law jurisdiction confirmation + any user-requested copy edits from the checkpoint).
</output>
