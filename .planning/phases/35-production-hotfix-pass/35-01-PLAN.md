---
phase: 35-production-hotfix-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/dto/wsSchemas.ts
  - server/llm/providerHealthState.ts
  - scripts/test-provider-health-state.ts
autonomous: true
requirements: []  # Wave 1 foundation: WS schema + dev-only outage injector that subsequent plans (35-02, 35-03, 35-05) depend on. No requirements directly closed here; this is pure scaffolding.
must_haves:
  truths:
    - "shared/dto/wsSchemas.ts validates inbound PROVIDER_DEGRADED and PROVIDER_RECOVERED messages on the client; unknown fields are rejected (strict allowlist via z.object(...).strict())"
    - "server/llm/providerHealthState.ts exposes a single in-memory sliding-window counter API that 35-02 wires into providerResolver.ts and 35-05 toggles via DEV-only injection"
    - "DEV-only outage injection refuses to take effect when NODE_ENV=production (production guard) — verified by scripts/test-provider-health-state.ts case 'rejects-injection-in-prod'"
    - "PROVIDER_DEGRADED payload shape contains ONLY {type, reason}; never includes provider identity, error stack, or model name (enforced by .strict() Zod modifier)"
    - "providerHealthState.ts exposes a recovery hook (registerRecoveryHook + forceRecoveryBroadcast) so 35-02 can wire the broadcaster and 35-05's /api/dev/force-recovery endpoint can deterministically broadcast PROVIDER_RECOVERED without an LLM round-trip"
  artifacts:
    - path: shared/dto/wsSchemas.ts
      provides: "Zod schemas for PROVIDER_DEGRADED + PROVIDER_RECOVERED in requiredServerSchemas union; type exports flow into WSServerMessage union. Both schemas use z.object({...}).strict() to reject unknown fields at the security boundary."
      contains: "z.literal('provider_degraded')"
    - path: server/llm/providerHealthState.ts
      provides: "Sliding-window failure counter (3 failures in 60s window), recordFailure(), recordSuccess(), shouldEmitDegraded(), shouldEmitRecovered(), getDegradedState(); DEV-only forceOutageMode() guarded by NODE_ENV check; recovery-hook registry: registerRecoveryHook(fn) + forceRecoveryBroadcast() (both DEV-only, throws/no-ops in production); test-only __resetForTests()."
      min_lines: 100
    - path: scripts/test-provider-health-state.ts
      provides: "Unit test suite for sliding-window counter (3-fail trigger, 60s window expiry, success-resets-counter, 429-doesn't-count-as-fail when next provider succeeds, prod-guards-injection, forceRecoveryBroadcast invokes registered hook)."
      min_lines: 110
  key_links:
    - from: shared/dto/wsSchemas.ts
      to: client/src/components/CenterPanel.tsx (consumed in 35-03)
      via: WSServerMessage type union
      pattern: "type:\\s*'provider_(degraded|recovered)'"
    - from: server/llm/providerHealthState.ts
      to: server/llm/providerResolver.ts (consumed in 35-02)
      via: named import { recordFailure, recordSuccess, shouldEmitDegraded, shouldEmitRecovered }
      pattern: "from.*providerHealthState"
    - from: server/llm/providerHealthState.ts (registerRecoveryHook + forceRecoveryBroadcast)
      to: server/routes.ts (35-02 wires the hook) + server/routes/health.ts (35-05 calls forceRecoveryBroadcast)
      via: named imports
      pattern: "registerRecoveryHook|forceRecoveryBroadcast"
    - from: scripts/test-provider-health-state.ts
      to: server/llm/providerHealthState.ts
      via: tsx invocation
      pattern: "import.*providerHealthState"
---

<objective>
Build the Wave 1 foundation for Phase 35: WS schema additions for the two new server-to-client events, and the in-memory provider-health state module that 35-02 wires into the resolver.

Purpose: Plans 35-02 (server emit), 35-03 (client toast), and 35-05 (Playwright spec) all depend on (a) the typed WS payload shape and (b) a single canonical counter implementation. Building these first prevents schema drift between server and client and gives 35-05 a deterministic injection point. The DEV-only outage injector solves AUDIT-01's "how do you trigger 3 chain failures in a Playwright spec without real provider outages" question. The recovery-hook scaffolding (registerRecoveryHook + forceRecoveryBroadcast) lets 35-02 own broadcaster wiring end-to-end while 35-05 owns only the DEV-only HTTP injection surface — clean wave boundaries.

Output: Two new typed WS events validated by Zod with `.strict()` modifier (shared/dto/wsSchemas.ts), one new server module (`server/llm/providerHealthState.ts`) implementing the sliding-window counter behind a clean API plus the recovery-hook registry, and one tsx test script proving the counter logic + the production guard on injection + the recovery-hook fires.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shashankrai/Documents/hatching-mvp-5th-march/.planning/phases/35-production-hotfix-pass/35-CONTEXT.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/CLAUDE.md
@/Users/shashankrai/Documents/hatching-mvp-5th-march/shared/dto/wsSchemas.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providerResolver.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/server/llm/providerTypes.ts
@/Users/shashankrai/Documents/hatching-mvp-5th-march/server/productionGuard.ts

<interfaces>
<!-- Use these contracts directly — do not explore the codebase to rediscover them. -->

From server/llm/providerTypes.ts (existing — DO NOT modify in this plan):
```typescript
export type ProviderId = 'openai' | 'gemini' | 'groq' | 'ollama-test' | 'mock';
```

From shared/dto/wsSchemas.ts (existing pattern to follow — see lines 153-170 for `project_created` shape and lines 232-237 for `upgrade_required` shape).

CRITICAL — existing schemas use the default Zod object mode which is `strip` (silently drops unknown keys). For Phase 35's PROVIDER_DEGRADED payload this is INSUFFICIENT — we explicitly want unknown keys to FAIL validation so a future server bug or downstream consumer attempting to leak `provider`/`model`/`error` fields is caught immediately. Use the `.strict()` modifier:
```typescript
// CORRECT — .strict() makes unknown fields fail safeParse:
z.object({
  type: z.literal('provider_degraded'),
  reason: z.string().min(1).max(200),  // server-overridable copy
}).strict(),
z.object({
  type: z.literal('provider_recovered'),
}).strict(),
```
Without `.strict()`, `safeParse({type:'provider_degraded', reason:'slow', provider:'openai'})` returns `{success: true}` with `provider` silently stripped — defeating the security boundary in Task 1's negative test.

From server/productionGuard.ts (existing pattern to mirror — 14 lines total):
```typescript
export function assertProductionStorageMode(nodeEnv?: string, storageMode?: string): void {
  if (nodeEnv === 'production' && storageMode !== 'db') {
    throw new Error('FATAL: ...');
  }
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PROVIDER_DEGRADED + PROVIDER_RECOVERED Zod schemas (with .strict()) to shared/dto/wsSchemas.ts</name>
  <files>shared/dto/wsSchemas.ts</files>
  <behavior>
    - PROVIDER_DEGRADED schema: `z.object({ type: z.literal('provider_degraded'), reason: z.string().min(1).max(200) }).strict()` — `.strict()` is REQUIRED; without it, Zod's default `strip` mode silently drops unknown fields and Task 1's negative test would FAIL.
    - PROVIDER_RECOVERED schema: `z.object({ type: z.literal('provider_recovered') }).strict()` — `.strict()` is REQUIRED for the same reason.
    - Both schemas are appended to the existing `requiredServerSchemas` z.union near the end (before the closing `]);`)
    - WSServerMessage type union exports update naturally via z.infer (no extra exports needed)
    - Negative test: a payload with `provider: 'openai'` field on PROVIDER_DEGRADED MUST fail validation (`.strict()` rejects extra keys; default `strip` mode would silently pass)
  </behavior>
  <action>
    Per D-10 and threat-model concern "don't leak provider identity in WS payload": add two new z.object literal-typed schemas to the `requiredServerSchemas` union in shared/dto/wsSchemas.ts. PROVIDER_DEGRADED carries ONLY `{ type, reason }` — explicitly NO provider field, no model field, no error stack. PROVIDER_RECOVERED is type-only.

    **Use `.strict()` on BOTH schemas — this is the security boundary.** The existing schemas in this file (e.g. `project_created` at lines 153-170) rely on Zod's default `strip` mode which silently drops unknown keys. That is unsafe for our two new schemas: a future server-side change that accidentally adds `provider: 'openai'` to a PROVIDER_DEGRADED payload would pass validation silently, leaking provider identity to the client. With `.strict()`, that change fails the negative test in this task's verify command.

    Use `z.string().min(1).max(200)` for `reason` (200-char ceiling prevents hostile-server amplification). Append to the union following the existing style — see how `upgrade_required` (lines ~232-237) and `usage_warning` are added.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit && npx tsx -e "import { wsServerMessageSchema } from './shared/dto/wsSchemas'; const positivePass = wsServerMessageSchema.safeParse({type:'provider_degraded',reason:'slow'}).success === true; const recoveredPass = wsServerMessageSchema.safeParse({type:'provider_recovered'}).success === true; const leakyReject = wsServerMessageSchema.safeParse({type:'provider_degraded',reason:'slow',provider:'openai'}).success === false; const recoveredLeakyReject = wsServerMessageSchema.safeParse({type:'provider_recovered',extra:'x'}).success === false; const ok = positivePass && recoveredPass && leakyReject && recoveredLeakyReject; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <done>
    Both schemas added to the union with `.strict()`; WSServerMessage type compiles; positive payload validates true and BOTH leaky payloads (extra `provider` field on degraded, extra `extra` field on recovered) validate false. The verify command exits 0 only when all four assertions hold.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create server/llm/providerHealthState.ts (sliding-window counter + DEV-only outage injector + recovery hook registry)</name>
  <files>server/llm/providerHealthState.ts</files>
  <behavior>
    - Module exports: `recordFailure(): boolean` (returns true if this failure crossed the 3-in-60s threshold AND we were not already degraded — caller emits PROVIDER_DEGRADED)
    - Module exports: `recordSuccess(): boolean` (returns true if we were degraded and now recover — caller emits PROVIDER_RECOVERED). Counter resets to empty array on success.
    - Module exports: `getDegradedState(): { isDegraded: boolean; reason: string }` (used at WS connect time to send a late-join PROVIDER_DEGRADED if a client connects mid-outage — 35-02 Task 4 calls this from wss.on('connection'))
    - Module exports: `forceOutageMode(enabled: boolean): void` — DEV-ONLY: throws if NODE_ENV === 'production'. When `enabled === true`, recordSuccess() is a no-op (calls still increment the failure counter as if injected). Used by 35-05 spec.
    - Module exports: `registerRecoveryHook(fn: () => void): void` — DEV-only registry. 35-02's wiring (in server/routes.ts onBroadcastReady handler) registers a hook that broadcasts `{type:'provider_recovered'}` to all sockets. In production, this is a silent no-op (belt-and-suspenders — production never needs forced recovery).
    - Module exports: `forceRecoveryBroadcast(): void` — DEV-only. Throws if NODE_ENV === 'production'. Calls recordSuccess() then invokes the registered recovery hook (if any). This is the deterministic recovery path 35-05's /api/dev/force-recovery endpoint uses (no real LLM round-trip needed).
    - Counter implementation: `let failureTimestamps: number[] = []` at module scope. recordFailure pushes Date.now(), filters out timestamps older than 60_000ms, returns `failureTimestamps.length >= 3 && !wasAlreadyDegraded`.
    - Module-scope `let isDegraded: boolean = false` — set true when recordFailure returns true; set false in recordSuccess.
    - Module-scope `let recoveryHook: (() => void) | null = null` — set by registerRecoveryHook, invoked by forceRecoveryBroadcast.
    - Default `reason` constant: `"Agents are slow right now, hang tight"` (matches D-07; server can override via 35-02 emit call).
  </behavior>
  <action>
    Create new module at server/llm/providerHealthState.ts. Implement per D-10 (3-in-60s sliding window), D-11 (recovery on next success), D-12 (success resets counter), and the threat-model concern "DEV-only mock-outage mechanism not exploitable in prod" — use the productionGuard.ts pattern (14-line shape) and throw a FATAL error if forceOutageMode() OR forceRecoveryBroadcast() is called when NODE_ENV === 'production'.

    **Recovery-hook scaffolding (clean wave boundary):** This module owns the recovery-hook registry so 35-02 can register the broadcaster end-to-end (35-02 owns server/routes.ts wiring) and 35-05 can call forceRecoveryBroadcast() from /api/dev/force-recovery without modifying any 35-01- or 35-02-owned files. Implementation:
    ```typescript
    let recoveryHook: (() => void) | null = null;

    export function registerRecoveryHook(fn: () => void): void {
      if (process.env.NODE_ENV === 'production') return;  // silent no-op in prod
      recoveryHook = fn;
    }

    export function forceRecoveryBroadcast(): void {
      if (process.env.NODE_ENV === 'production') {
        throw new Error('FATAL: forceRecoveryBroadcast called in production');
      }
      recordSuccess();
      try { recoveryHook?.(); } catch { /* never let dev hook break the response */ }
    }
    ```

    Implementation choices (Claude's discretion per CONTEXT.md): use a plain `number[]` for timestamps (not a ring buffer — N=3 is too small for ring-buffer optimization). Use `Date.now()` for monotonic-enough timestamps in this single-process counter (per D-10 "in-memory, per-server-instance"). Module-level mutable state is acceptable here because there's exactly one provider chain per server process.

    Export `__resetForTests()` (only when NODE_ENV !== 'production') to let scripts/test-provider-health-state.ts reset between cases. `__resetForTests()` MUST also clear `recoveryHook` (set back to null) so test isolation is complete. Mirror the productionGuard.ts pattern: simple imperative function, no class, no factory.

    Do NOT call providerResolver.ts from this module — it must be a leaf dependency to avoid circular imports. 35-02 will import THIS module from providerResolver.ts AND from server/routes.ts (for registerRecoveryHook wiring).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsx scripts/test-provider-health-state.ts</automated>
  </verify>
  <done>
    Module exists at server/llm/providerHealthState.ts; all seven exports (`recordFailure`, `recordSuccess`, `getDegradedState`, `forceOutageMode`, `registerRecoveryHook`, `forceRecoveryBroadcast`, `__resetForTests`) present; test script (Task 3) passes all cases. typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write scripts/test-provider-health-state.ts unit test suite</name>
  <files>scripts/test-provider-health-state.ts</files>
  <behavior>
    Test cases (each prints PASS/FAIL line and exits non-zero on first FAIL):
    1. "3 failures in window → degraded" — record 3 failures within 1s; assert recordFailure on the 3rd returns true, getDegradedState().isDegraded === true.
    2. "2 failures don't trigger" — record 2 failures; assert all return false; isDegraded === false.
    3. "Window expiry resets" — record 2 failures, advance time (mock Date.now via vi-style or by manipulating module state via __resetForTests + manual injection helper), record 1 more 61s later; assert it does NOT trigger (only 1 failure in window).
    4. "Success resets counter" — record 3 failures (now degraded), call recordSuccess; assert returns true, isDegraded === false; record 2 more failures, assert NOT degraded again (counter reset).
    5. "Repeat-degraded does not re-emit" — record 3 failures, record a 4th in same window; recordFailure on 4th returns false (already degraded → no duplicate event).
    6. "forceOutageMode rejected in prod" — set process.env.NODE_ENV = 'production', call forceOutageMode(true); assert throws; restore NODE_ENV.
    7. "forceOutageMode allowed in dev" — set process.env.NODE_ENV = 'development', call forceOutageMode(true); assert no throw; recordSuccess does nothing while injection active.
    8. "forceRecoveryBroadcast rejected in prod" — set NODE_ENV='production', call forceRecoveryBroadcast(); assert throws; restore.
    9. "forceRecoveryBroadcast invokes registered hook" — set NODE_ENV='development', __resetForTests(), 3x recordFailure (now degraded), let `let hookCalls = 0;`, registerRecoveryHook(() => hookCalls++), call forceRecoveryBroadcast(); assert hookCalls === 1, isDegraded === false.
    10. "registerRecoveryHook silent no-op in prod" — set NODE_ENV='production', registerRecoveryHook(() => {}); assert NO throw (silent no-op per spec). Restore NODE_ENV.
  </behavior>
  <action>
    Mirror the existing test-script pattern at scripts/test-budget-race.ts and scripts/test-abort-cleanup.ts (referenced in plan 28-01-PLAN.md). Use `tsx` (already in devDeps), no external test framework. Each case is a top-level `async function caseName()` block; main() runs them sequentially; first failure throws and exits 1.

    For window-expiry timing (case 3): instead of real-time `await sleep(61000)`, expose a test-only helper from providerHealthState.ts (`__advanceWindowForTests(ms: number)` or `__injectTimestampsForTests(arr: number[])`) so the test runs fast (<1s total). Document the helper as DEV-only and gate it on NODE_ENV !== 'production'.

    For case 6/8/10 (NODE_ENV manipulation): save and restore process.env.NODE_ENV around each call so subsequent cases see correct state. Use try/finally to guarantee restoration even on assertion failure.

    For case 9 (recovery hook): __resetForTests() between cases ensures the previously-registered hook from prior cases is cleared. This validates that __resetForTests() clears `recoveryHook` to null (a behavioral check on Task 2's implementation requirement).

    The script's exit code is the verification signal for execute-phase.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsx scripts/test-provider-health-state.ts</automated>
  </verify>
  <done>
    All 10 cases print PASS; script exits 0; running it twice in a row gives identical output (deterministic).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → WS message | Client receives PROVIDER_DEGRADED from server; payload must not leak provider identity (info-disclosure risk) |
| dev/test → forceOutageMode + forceRecoveryBroadcast | DEV-only injection mechanisms must NOT be reachable in production (else attackers could DoS the toast UI or silently dismiss real degradation) |
| dev/test → registerRecoveryHook | DEV-only registry must be a silent no-op in production so a misbehaving import in prod cannot register an attacker-controlled callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | shared/dto/wsSchemas.ts PROVIDER_DEGRADED + PROVIDER_RECOVERED schemas | mitigate | Both schemas use the explicit `z.object({...}).strict()` modifier — Zod's default `strip` mode silently drops unknown keys, which would let a future server-side bug pass `provider:'openai'` through validation undetected. With `.strict()`, extra fields cause `safeParse({...}).success === false`. Negative test in Task 1 verifies extra `provider` field on degraded AND extra field on recovered are BOTH rejected. No `.passthrough()` anywhere. |
| T-35-02 | Tampering / Elevation | server/llm/providerHealthState.ts forceOutageMode() + forceRecoveryBroadcast() | mitigate | Both functions throw FATAL if NODE_ENV === 'production'. Mirrors server/productionGuard.ts pattern. forceOutageMode verified by Task 3 case 6; forceRecoveryBroadcast verified by Task 3 case 8. |
| T-35-03 | Denial of Service | reason field in PROVIDER_DEGRADED payload | mitigate | z.string().max(200) prevents amplification if a future server bug formats unbounded error text into the reason. |
| T-35-04 | Repudiation | Counter state lives in-memory only | accept | Per D-10 "per-server-instance, in-memory (sufficient for MVP single-node deploy)". Audit trail via existing `provider_fallback` autonomy_event row in chat.ts:2297-2317 covers forensics. |
| T-35-21 | Tampering | registerRecoveryHook callable in production | mitigate | Function is a silent no-op in production (early return when NODE_ENV === 'production' before recoveryHook is set). Verified by Task 3 case 10. forceRecoveryBroadcast — the only consumer of recoveryHook — also throws in prod, so even if the hook were somehow set, it cannot be triggered. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes (TypeScript strict mode green)
2. `npx tsx scripts/test-provider-health-state.ts` exits 0 with all 10 PASS lines
3. Manually parse a sample PROVIDER_DEGRADED payload through wsServerMessageSchema in node REPL — strict mode rejects extra fields
4. `grep -v '^//' server/llm/providerHealthState.ts | grep -c "process.env.NODE_ENV"` returns ≥ 3 (production guard present in forceOutageMode + registerRecoveryHook + forceRecoveryBroadcast)
5. `grep -c "\.strict()" shared/dto/wsSchemas.ts` returns ≥ 2 (both new schemas use .strict())
</verification>

<success_criteria>
- shared/dto/wsSchemas.ts has exactly two new schemas in the requiredServerSchemas union (PROVIDER_DEGRADED, PROVIDER_RECOVERED) with `.strict()` modifier on both
- server/llm/providerHealthState.ts exists, exports recordFailure / recordSuccess / getDegradedState / forceOutageMode / registerRecoveryHook / forceRecoveryBroadcast / __resetForTests
- forceOutageMode + forceRecoveryBroadcast both throw when NODE_ENV === 'production'
- registerRecoveryHook is a silent no-op when NODE_ENV === 'production'
- All 10 unit-test cases in scripts/test-provider-health-state.ts pass
- No changes to server/llm/providerResolver.ts in this plan (35-02 owns that)
- No changes to server/routes.ts in this plan (35-02 owns the registerRecoveryHook wiring)
- No changes to client code in this plan (35-03 owns that)
</success_criteria>

<output>
After completion, create `.planning/phases/35-production-hotfix-pass/35-01-SUMMARY.md` recording: WS event payload shapes (with .strict() modifier note), provider-health-state module API (including the recovery-hook registry), and which subsequent plans (35-02 wires registerRecoveryHook, 35-05 calls forceRecoveryBroadcast) consume what.
</output>
</content>
</invoke>